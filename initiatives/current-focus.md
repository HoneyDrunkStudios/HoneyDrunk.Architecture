# Current Focus

The ranked priority list for the HoneyDrunk Grid — what the team (human + agents) should work on next, **in order**.

**Last reviewed:** 2026-06-09 (Operator follow-up to the loop-engineering reconcile — final adjustments applied. Two prior ranked rows were confirmed **decision-record / catalog bookkeeping, not open build work**, and are removed from the ranked band and folded into the Archive / exit-criteria / reconcile sweep (#9): (a) the standalone "accept ADR-0091/0092 to reconcile the shipped HoneyHub cockpit" row — **HoneyHub v1 has shipped**; ADR-0091/0092 are conceptually accepted and only un-flipped, so their acceptance is reconciliation bookkeeping, not open build; and (b) the ADR-0077 IaC row — per operator the **Bicep IaC work is fully done and tested** (*not* "pending verification"); its only residual — `HoneyDrunk.Infrastructure` not yet in `nodes.json`, repo-scaffold packet 11 unfiled — is catalog/registration bookkeeping that also folds into #9. Removing those two ranked rows dropped the table toward 10; held at a comfortable **12** (10–15 band, off the floor) by promoting genuine Grid-state candidates: **ADR-0042 Idempotency Contract** (#6 — Accepted-decision substrate that gates the ADR-0047 Tier-2b idempotency-store contract tests at #4) and **ADR-0027 Notify Cloud Node scaffold packet filing** (#7 — the unfiled `06-notify-cloud-node-scaffold.md` anomaly is the actionable precursor that unblocks the Notify Cloud kickoff at #8, replacing the now-removed ADR-0077 provisioning blocker). Prior reconcile still stands: ADR-0093 Tier-A substrate shipped on branch `claude/work-prioritization-6t11r8` (commit 4f3baf6, not a PR) → exit-review (#1); ADR-0043 is **Accepted** (#3); active build leverage stays on the loop-engineering follow-through plus the testing/review/idempotency substrate and the Notify Cloud kickoff chain.)
**Review cadence:** weekly (per ADR-0043 briefing)
**Related:** per-initiative detail in [active-initiatives.md](active-initiatives.md); quarterly horizon in [roadmap.md](roadmap.md).

Rank is strict ordinal — no ties. Edit ranks directly in this table; the order *is* the decision. Cap is the top 15; everything tracked but lower lives in **Future / Watch** below.

## Ranked Priorities

| # | Item | Type | Status | Phase | Due | Why now | Exit criteria | Blocked by |
|---|------|------|--------|-------|-----|---------|---------------|------------|
| 1 | ADR-0093 Loop Engineering — Tier-A substrate exit-review | adr-acceptance + initiative (ADR-0093) | **Accepted — Tier-A substrate shipped (commit 4f3baf6); exit-review** | **Tier A of 3 (A done; B/C sequenced)** | 2026-06-15 (target) | **Operator priority (2026-06-07): the substrate for running many product loops in parallel — the charter's AI-multiplier bet made operational.** The substrate landed directly on this branch (doctrine `constitution/loop-engineering.md`, the `loops/` registry + `LDR-TEMPLATE.md`, six backfilled LDRs `loop-0001`–`loop-0006`, runner loop-job convention, capability-matrix loop-owner note, LDR-id naming rule). What remains is **not** the substrate — it is exit-review/archive of the Tier-A initiative and registering the HoneyHub Loop Console as a future program phase. The two higher autonomy tiers are separate rows: **Tier B is #2 (gated on Evals/ADR-0023)**; the Loop Console is F/W (gated on HoneyHub v1 / ADR-0091/0092). | ADR-0093 Tier-A initiative entered in `active-initiatives.md` and walked to exit-review; HoneyHub Loop Console registered as a future phase in `initiatives/programs/honeyhub.md`; no substrate packets left open | None — substrate is on-disk |
| 2 | Begin ADR-0023 Evals standup — the Tier-B loop-autonomy gate | adr-acceptance + initiative (ADR-0023) | Proposed (packets 01–04 + dispatch-plan authored) | **AI-seed standup** (scaffold) | 2026-08-31 (target) | **The actual unlock for parallel products and the highest-leverage *new build* now that HoneyHub v1 has shipped and ADR-0077 IaC is done and tested.** Loop engineering (#1) reframes Evals from "gated on a product eval need" (old Q4 forcing function) to *the gate that lets a loop run without the operator as the per-step bottleneck* (ADR-0093 D4 Tier B / Prerequisites table). Evals is an AI-seed Node that stands up independently of the other 8 AI Nodes; its scaffold packets already exist. Pulled forward from Q4. | ADR-0023 Accepted; `HoneyDrunk.Evals` scaffolded (Abstractions `IEvaluator`/`IEvalScorer`/`IEvalSuite`/`IEvalTarget` + InMemory) sufficient to serve as a loop gate per ADR-0093 Tier B | #1 (loop substrate defines the Tier-B gate contract; can scaffold in parallel) |
| 3 | ADR-0043 Backlog Generation — Phase 1 first live run | initiative (ADR-0043) | **Accepted — automation substrate landed; first live run + scheduler registration pending** | **Phase 1 of 4** (substrate done; run pending) | 2026-07-15 (target) | ADR-0043 is **Accepted** (old row said Proposed — stale); the generated surfaces, invariants, agent-prompt amendments, and ADR-0086 job specs all shipped. What remains is mechanical-but-load-bearing: register the scheduled tasks on the runner host and execute the **first live Strategic run** so accepted-but-unimplemented decisions (the long ADR-0048–0078 Proposed tail, ADR-0046/0047 follow-ons) auto-generate proposed packets. This is the source that *feeds the loops* in #1/#2 — it ranks right behind them. | Scheduled tasks registered on the runner host; first live Strategic source run creates (or explicitly dedupe-skips with evidence) proposed packets for Accepted decisions with missing coverage | #1 (loops doctrine frames the backlog-gen sources as LDRs) |
| 4 | ADR-0047 Phase 2 — Tier 2a integration CI | initiative (ADR-0047) | In progress (Phase 1 shipped; Phase 2 not started) | **Phase 2 of 6** (Phase 1 complete; tracked in `active-initiatives.md`) | 2026-07-31 (target) | Foundational substrate. Closes ADR-0011 Gap 1; unblocks ADR-0042 contract tests; gives ADR-0044 D3 cat 11 (Testing Quality) something concrete to enforce; and is a prerequisite for the ADR-0093 build-loop gate (a loop that opens PRs needs integration CI to be a trustworthy automated gate). Phase-1 scaffold template (#190) and review checklist (#191) already landed. | `job-integration-tests.yml` live in Actions and wired into `pr-core.yml`; Standards ships the shared unit-test-stack props + coverlet templates + `Thread.Sleep` analyzer (packets 01–03) | None |
| 5 | Land ADR-0046 — Specialist Review Agents (Phase 1) | adr-acceptance + packet | Proposed (acceptance packet open as Architecture#262) | **Phase 1 of 5** | 2026-07-15 (target) | Establishes the specialist-agent pattern. The `cfo` agent has retroactive value on every cost-touching ADR/PDR — and now that ADR-0052 cost-governance is Accepted, the `cfo` agent has a concrete policy surface (budgets, kill-switches, cost categories) to reason against. Also a natural loop-owning-agent class under ADR-0093. | ADR-0046 Accepted (Architecture#262); Phase 1 (`.claude/agents/cfo.md` authored + retroactively invoked against a cost-touching PR) shipped; `agent-capability-matrix.md` updated | None |
| 6 | Accept ADR-0042 — Idempotency Contract for Async Boundaries | adr-acceptance + packet | Proposed (acceptance packets #248/#249 open) | **Phase 1 of 2** (contract; backing parked) | 2026-07-31 (target) | Substrate the testing rollout already depends on: ADR-0047 Phase 3 packet 11/12 (Kernel `IIdempotencyStore` reusable contract test + InMemory binding) is **parked until ADR-0042 is Accepted** — so the idempotency contract gates the integration-CI work at #4 and the Kernel contract-test pilot. It is also the at-least-once-delivery guarantee any autonomous build/maintenance loop (ADR-0093) leans on when it retries. Accept the Phase-1 contract surface (`IIdempotencyKeyProvider`/`IIdempotencyStore` + InMemory) now; the Cosmos backing stays parked behind a Kernel release. | ADR-0042 Accepted (#248/#249); `IIdempotencyStore` Phase-1 contract + InMemory landed in Kernel.Abstractions sufficient to back the ADR-0047 packet-11 reusable contract test; Cosmos backing explicitly deferred | None |
| 7 | File the ADR-0027 Notify Cloud Node scaffold packet — Notify Cloud precursor | housekeeping + packet | **Anomaly: packet `06-notify-cloud-node-scaffold.md` has no filed issue state** | n/a | 2026-07-15 (target) | Blocker-row for #8: `proposed-adrs.md` flags the ADR-0027 scaffold packet (`06-notify-cloud-node-scaffold.md`) as having no filed issue state. The Notify Cloud kickoff slice (#8) accepts the ADR-0027 Notify Cloud Node and scaffolds tenant isolation — but it cannot execute the Node standup wave until this scaffold packet is filed as an issue. With the old ADR-0077 verification blocker removed (folded to #9), filing this packet is the real actionable precursor that unblocks #8. | `06-notify-cloud-node-scaffold.md` filed as a GitHub issue (operator/scope step); `adr-0027-notify-cloud-standup` initiative entry present in `active-initiatives.md` with the packet checklist | None |
| 8 | Notify Cloud (PDR-0002) — multi-tenant scaffolding kickoff slice | pdr-acceptance + packet | Proposed | **kickoff slice only** | 2026-07-31 (target) | The roadmap's named product thread (PDR-0002, the Grid's first commercial trial). Its provisioning dependency (ADR-0077 reproducible Bicep) is **fully done and tested** per operator — not "pending verification" (the only catalog-registration residual is bookkeeping folded into #9), so the cloud bring-up no longer waits on any provisioning work. Kickoff slice only — accept the ADR-0027 Notify Cloud Node and begin tenant-isolation scaffolding at the gateway/queue/Vault layers. Full PDR-0002 commercial milestones (REST API, client NuGet GA, first paying tenant) stay Q3. | ADR-0027 Accepted (Notify Cloud Node standup); multi-tenant scaffolding spike landed at gateway/queue/Vault; Stripe-wiring scope drafted | #7 (ADR-0027 Node scaffold packet must be filed before the standup wave can execute) |
| 9 | Archive / exit-criteria review + decision-record reconcile sweep | housekeeping + adr-acceptance | Pending | n/a | 2026-06-15 (target) | Many rollouts are 100% closed; leaving them in active tracking inflates apparent active work. Adds the ADR-0093 Tier-A initiative (#1, once exit-reviewed) and the just-completed threads. **Now also absorbs two decision-record reconciliations the operator confirmed are bookkeeping, not build:** (a) flip **ADR-0091/0092** to Accepted to match the *shipped* HoneyHub v1 cockpit (conceptually accepted, only un-flipped); (b) close the **ADR-0077** catalog residual — `HoneyDrunk.Infrastructure` is **already registered** in `nodes.json` (node `honeydrunk-infrastructure`), so this is exit-review/verification only, not build; the IaC work itself is **fully done and tested**. | ADR-0005/0006, ADR-0009, ADR-0014, ADR-0015, ADR-0030, ADR-0033, ADR-0044, ADR-0080 (verify archived), ADR-0086, ADR-0088, ADR-0093 (Tier A), Lore, Vault.Rotation, Kernel Adoption Alignment moved to `archived-initiatives.md`; **ADR-0091/0092 flipped to Accepted against the shipped cockpit; `HoneyDrunk.Infrastructure` registration in `nodes.json` confirmed already present — ADR-0077 catalog residual closed**; ADR-0083/0084 exit-criteria reviewed | None |
| 10 | Reconcile ADR-0032 acceptance — `accepts:` frontmatter or manual flip | housekeeping + adr-acceptance | **Proposed (12/12 packets closed; ADR not auto-flipped)** | n/a | 2026-06-30 (target) | ADR-0032 PR Validation Policy is **fully implemented** — all 12 coverage-gate/NuGet-flagging packets closed 2026-05-19/20 — but the ADR is stuck Proposed because the packets never declared `accepts:` frontmatter, so hive-sync won't auto-flip it. A shipped-but-Proposed ADR is exactly the drift the loop-engineering doctrine and ADR-0043 reactive source exist to catch. Cheap, honest cleanup: either backfill `accepts:` on a packet or manually flip the status. | ADR-0032 flipped to Accepted (manual or via reconciled `accepts:` frontmatter); initiative walked to exit-review | None |
| 11 | ADR-0023 Evals — register the Evals standup initiative + reconcile packet anomaly | housekeeping | **Anomaly: packet `04-evals-node-scaffold.md` has no filed issue state** | n/a | 2026-08-15 (target) | Surfaced as a blocker-row for #2: `proposed-adrs.md` flags the ADR-0023 scaffold packet (`04-evals-node-scaffold.md`) as having no filed issue state — it must be filed before the Evals standup (#2) can execute its scaffold wave. Filing the issue + registering the `adr-0023-evals-standup` initiative in `active-initiatives.md` is the actionable precursor that unblocks #2's scaffold. | `04-evals-node-scaffold.md` filed as a GitHub issue (operator/scope step); `adr-0023-evals-standup` initiative registered in `active-initiatives.md` with the packet checklist | #2 conceptually depends on this; filing is operator/scope-owned |
| 12 | ADR-0084 Phase 1 emitter remainder — CI-on-main / release events → Discord | packet | Accepted; Phase 1 core shipped; remainder is a clean follow-up | **Phase 1 remainder** | 2026-07-31 (target) | ADR-0084 is Accepted and the seam (`job-discord-notify.yml`) + credential-escalation emitter are live. The Phase-1 remainder — wiring CI-on-main failure and release/NuGet/deploy events to `#release`/`#ops-alerts` — is explicitly called a "cleanly wireable follow-up" and is exactly the operator-visibility a parallel-loop posture (ADR-0093) wants before loops run unattended. Phase 3/4 vendor-webhook emitters stay F/W (substrate-gated). | CI-on-main failure + release/NuGet/deploy events emit to Discord via `job-discord-notify.yml`; no Phase 3/4 vendor-webhook scope pulled in | None |

## Type Legend

The `Type` column must use exactly one of these values (combinations like `adr-acceptance + packet` are allowed when a row covers acceptance *and* a discrete shipping packet — the common case for ADRs whose Phase 1 is a single packet).

| Type | When to use | Example |
|------|-------------|---------|
| `adr-acceptance` | Accept a Proposed ADR (`adrs/ADR-*.md`). Often paired with that ADR's Phase 1 implementation. | "Land ADR-0044 — Cloud Code Review (Phase 1)" |
| `pdr-acceptance` | Accept a Proposed PDR (`pdrs/PDR-*.md`). | "Land PDR-0008 — Curiosities" |
| `bdr-acceptance` | Accept a Proposed BDR (`business/decisions/BDR-*.md`) — Business Decision Records, same shape as ADRs/PDRs but scoped to operations. | "Land BDR-0002 — Mailbox provider switch" |
| `packet` | One or a few discrete GitHub issue packets, not part of a tracked multi-packet initiative. | "ADR-0033 environment-gated trigger packets" |
| `initiative` | Multi-packet rollout tracked in `active-initiatives.md`. Annotate the governing ADR/PDR in parentheses, e.g. `initiative (ADR-0015)`. | "Deploy Notify + Pulse to dev" |
| `operational` | Non-decision-driven ops work (deploys, incident response, infra bring-up) where no ADR/PDR/BDR governs the activity. | "Rotate Vault signing keys" |
| `housekeeping` | Repo hygiene, archives, doc cleanups, drift fixes. | "Archive / exit-criteria review" |

## Future / Watch

Tracked but below the active top-15. Promote into the table when a forcing function fires.

- **ADR-0034 — Public Package Distribution and NuGet Policy** *(Watch — demoted 2026-05-31)* — Proposed; acceptance packets #208/#209/#210 + Standards#40 + Actions#94 + Vault#34 filed. **Demoted out of the ranked band because its forcing function — "first external NuGet consumer" — has not fired.** The earlier promotion rationale (ADR-0082 D8 / ADR-0083 D2 *reference* `NUGET_API_KEY`) is a documentation cross-link, not a live consumer pulling the publishing pipeline into existence. Promote when Notify Cloud's `HoneyDrunk.Notify.Client` NuGet (PDR-0002 Q3) or another real external consumer actually needs to ship a package.
- **ADR-0052 residual — `ICostLedger` → Kernel relocation (Architecture#355)** *(Watch — AI-gated)* — ADR-0052 is **Accepted** (wave-1 governance #354/#356/#357/#358/#359 closed 2026-05-30); the residual #355 is the catalog/contract-relocation packet that is hard-gated on AI-side work (HoneyDrunk.AI is at seed v0.1.0; ADR-0016 Phase-1 scaffold never executed) and a human Kernel release. Tracked as gated in `active-initiatives.md` under the ADR-0052 entry, not as a current-focus line.
- **ADR-0084 Phase 3/4 vendor-webhook emitters** *(Watch — substrate-gated)* — ADR-0084 is **Accepted** (core + seam + Phase 1 credential-escalation emitter shipped via #551/#178/#180, 2026-05-31). The remaining Phase 3/4 emitters (Dependabot/CodeQL/secret-scan, CodeRabbit P0/P1, Azure budget, App Insights error spikes) were designed as home-server webhook bridges under the now-superseded ADR-0081 OpenClaw premise; they need a substrate decision (the ADR-0086 runner absorbs them, or an Azure Function receives the webhooks) before wiring. GitHub-native security alerts (email + Security tab) cover the gap meanwhile. Promote when the substrate decision lands (ties to ADR-0088 cleanup + ADR-0086 Phase C/D).
- **Commercial / substrate ADRs (0035–0042, 0045)** — all Proposed, but scope packets filed 2026-05-22/23 (#240–#269) so the implementation backlog is sized. Each ADR is gated on a forcing function: 0035 on first external NuGet consumer; 0036 on first paying tenant; 0037 on Notify Cloud GA; 0038 on first non-Studio sender; 0040/0045 on AI-sector trace volume; 0041/0042 on AI-sector standup. ADR-0046 was promoted out of this group (see rank #5); ADR-0042 was promoted out as well (see rank #6); ADR-0034 was demoted *back* into Watch (see the dedicated bullet above). See `initiatives/proposed-adrs.md`.
- **AI-sector standup readiness (Agent Kit)** — Foundation for AI-powered Grid workflows (Capabilities, Agents, Memory). **0/9 standup ADRs Accepted** (ADRs 0017–0025). Gated on AI-sector standup ADRs moving toward acceptance; promote into the table when the first standup ADR is Pending Flip. *(2026-06-07: **ADR-0023 Evals is pulled out of this group** and promoted into the active table — now **rank #2** (the standup) with **#10** as its packet-filing precursor — because loop engineering (ADR-0093, now Accepted) gives Evals an independent, earlier forcing function (the Tier-B loop-autonomy gate) ahead of the rest of the AI sector.)*
- **Newly proposed ADRs 0048–0057** (drafted 2026-05-23) — tracked in `proposed-adrs.md`; promote into the table only after triage.
- **Canary test coverage expansion** — broaden canary coverage across Node boundaries; folds into ADR-0047's canary formalization (D8). ADR is Accepted; the D8 implementation packets are part of the ADR-0047 Phase 6 / follow-up scope.
- **ADR-0010 Phase 2 — Observe + AI routing scoping** *(Watch)* — Phase 2 of 3 (Phase 1 complete; Phase 3 blocked on HoneyHub Phase 2 read layer landing in Q3 2026). Promote when there is a concrete external-project observation need and a live application-code caller for cost-first routing. No concrete trigger yet.
- **HoneyHub build work** — the Grid's lead 2026 product thread. v1 re-scoped to the **Agent-Cockpit wedge** (PDR-0011, Accepted); the internal read-layer (PDR-0009) becomes a *later* layer, not v1. The cockpit's implementing decision — the **local-runner-bridge ADR (now ADR-0090, Accepted)** — is done, and **v1 has shipped**. The only residual is decision-record reconciliation bookkeeping: **ADR-0091/0092 are conceptually accepted and only un-flipped**, so flipping them to match the shipped cockpit is captured inside the **#9 reconcile sweep**, not as standalone build work. PDR-0009's read layer + generic Node shell is the Q3 deliverable; dispatch actions via PRs is Q4. The **HoneyHub Loop Console** (ADR-0093 D9) is a future HoneyHub-program phase, gated on v1 shipping — now promotable once the v1 ship is reconciled. ADR-0003 Phase 4 UI work composes into these phases.
- **ADR-0093 Loop Engineering — Tier B/C and the Loop Console** *(Watch — sequenced)* — Tier-A substrate is shipped (#1). **Tier B (eval-gated loops)** is the Q3 slice that actually unlocks parallel autonomous loops; it promotes once Evals (#2) scaffolds as the gate. **Tier C (self-tuning)** and the **HoneyHub Loop Console (D9)** stay Watch — Tier C needs Tiers A+B proven plus loop-health telemetry; the Console needs HoneyHub v1 shipped (which it now is — the ADR-0091/0092 acceptance is reconciliation bookkeeping in #9, no longer a build blocker).

## How to use this file

- The **rank** column is the decision. Reorder rows to re-prioritize; the table is the source of truth.
- **Target 15 items, but never pad with filler.** When one ships or is removed, refill by promoting the highest-justified candidate from the **Promotion sources** below. A slot left empty *because a forcing function genuinely has not fired* is an honest signal, not a missed decision — see the F/W note on shrinking. As of the 2026-06-09 operator follow-up the table runs at **12** — within the 10–15 band. HoneyHub v1 shipped and ADR-0077 IaC is fully done and tested, so both left the ranked band; their only residuals are decision-record/catalog bookkeeping folded into the #9 reconcile sweep. The active build leverage concentrates on the loop-engineering follow-through (#1–#3) plus the testing/review/idempotency substrate (#4–#6) and the Notify Cloud kickoff chain (#7–#8); the lower rows (#9–#12) are honest exit-review/reconciliation work, not filler. No padding was added beyond defensible, concretely-scoped items.
- **Promotion sources**, in the order netrunner consults them:
  1. **Future / Watch** below — items already tracked here whose forcing function has fired
  2. `proposed-adrs.md` — Proposed ADRs/PDRs in **Pending Flip** or **In Progress** that warrant attention
  3. ADR-0043 backlog generation output (when its Phase 1 lands): Strategic / Reactive / Tactical / Opportunistic packets
  4. Open Hive board items not currently tracked anywhere on this list or in F/W
  5. Operator's idea backlog (raised verbally; capture into Future / Watch first, then promote if justified)
- **Future / Watch is not fixed-size.** Promoting an item *out* of F/W does not require backfilling F/W with a new item — F/W can shrink. F/W grows organically when new ADRs/PDRs/BDRs are proposed, items are demoted from top-15, or new work is identified. Its purpose is visibility into tracked-but-not-active work, not a fixed bench. If F/W is empty *and* the other promotion sources are empty *and* the top-15 still has 15 items, the system is genuinely caught up — flag it as a finding rather than padding F/W with filler.
- **Each row is a single phase or actionable item**, not a whole multi-phase rollout. If Phase 1 of an ADR ships and Phase 2 isn't top-15-urgent, **drop the ADR off the list entirely** — phase tracking lives in [active-initiatives.md](active-initiatives.md), not here. Phase 2 can return to the list later if a forcing function fires.
- **Items can be** ADR acceptances, PDR acceptances, BDR acceptances, individual packets, multi-packet initiatives, or one-off operational/housekeeping work. The table is not ADR-only. See **Type Legend** below for the canonical list.
- **Phase** shows the specific phase or progress slice this row represents (e.g., `Phase 2 of 6`, `2/5 packets`, `0/9 standup ADRs`) or `n/a` for non-phased items. Full phase tracking is in [active-initiatives.md](active-initiatives.md); this cell is just a quick orientation hint.
- **Due** is the target ship date for this row, formatted `YYYY-MM-DD (hard)`, `YYYY-MM-DD (target)`, or `—`.
  - `(hard)` means the date is set by an external constraint that does not move (vendor launch, billing window, partner deadline, regulatory cutoff). Missing it has a real cost. The "Why now" cell should name the external constraint.
  - `(target)` means a self-imposed pacing date. Made up by the curator to keep work moving and to align the roadmap. Slippable, but each slip is a prioritization signal that should be noticed at the weekly review.
  - `—` is reserved for Watch rows where no trigger has fired and no honest date can be set. Do not use `—` for active work — guess a target instead.
  - Roadmap alignment: an item's quarter on [roadmap.md](roadmap.md) must include its Due date — Q2 = Apr–Jun, Q3 = Jul–Sep, Q4 = Oct–Dec. If you move an item's Due across a quarter boundary, move its roadmap line too.
- **Why now** must justify the rank — if it can't, the item probably isn't a top-15 priority.
- **Exit criteria** must be concrete and observable, not "feels done."
- **Blockers get promoted.** If item X is blocked by item Y and Y is itself actionable, **Y is its own row higher than X**, and X's "Blocked by" cell references Y by rank (e.g., `#2`). Non-actionable blockers (upstream CVEs, "no concrete trigger yet") stay in X's "Blocked by" cell only.
- Reviewed weekly at the ADR-0043 briefing; bump `Last reviewed` on every pass so stale-priority rot is visible.
- Per-initiative detail (packet checklists, board links, scope) lives in [active-initiatives.md](active-initiatives.md), not here.
