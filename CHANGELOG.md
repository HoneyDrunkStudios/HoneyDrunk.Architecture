# Changelog

## Unreleased

- Cut over the Architecture review pilot config from `runner: openclaw-codex` to ADR-0086 `runner: local-worker`, removed obsolete OpenClaw webhook inputs from the caller workflow, and refreshed the pilot evidence/schema docs for the label/comment queue.
- Added the ADR-0086 Grid Agent Runner framework under `infrastructure/workers/grid-agent-runner/`, including PowerShell entry points, portable job-spec validation, review/scheduled job specs, Task Scheduler registration scripts, dry-run smoke testing, and operator setup docs.
- Authored the six ADR-0082 Wave 3 per-class standup walkthroughs in `infrastructure/walkthroughs/` (D7): `node-standup-core-dotnet.md` (Abstractions+Runtime and Runtime-only, with the throwaway-breaking-change canary-confirmation ritual), `node-standup-ops-deployable-dotnet.md` (inherits Core .NET, adds Key Vault / App Configuration / managed identity / Container Apps / Bicep / ADR-0033 deploy trigger / health endpoints), `node-standup-meta-docs.md` (reduced procedure plus the content-shape canary for content-shipping Meta repos), `node-standup-ai-seed.md` (Phase A only plus the promotion gate to Core .NET), `node-standup-studios-typescript.md` (Node.js stack replacements, npm publishing, Web.UI design-token consumption), and `org-secret-repo-binding.md` (the GitHub portal click-path for binding new repos to org Actions secrets, with the stop-condition on unexpected access policy). Each walkthrough composes against `constitution/node-standup.md` rather than re-deriving the rules. Completes the ADR-0082 initiative.
- Authored `constitution/node-standup.md` (ADR-0082 D1, Wave 2) — the canonical Node standup procedure document and the load-bearing deliverable of ADR-0082. Captures the three-phase prerequisite chain (Phase A Architecture registration → Phase B human-only GitHub repo creation → Phase C agent-eligible scaffold landing, with the optional Phase D hive-sync reconciliation), the eighteen mandatory steps for every Node class, the class-specific steps a–z keyed to the six-class `node_class` taxonomy, and the per-class org-secret binding matrix (dated 2026-05-25 snapshot, updated via ADR-0083 D5/D6 inventory passes without ADR amendment). Inlines the rule text for every referenced invariant and the decision-specific summary for every referenced ADR so future standup packets dereference one self-contained source. Includes the operator-internal-automation carve-out (Key Vaults, GitHub Apps, OpenClaw schedules, credential inventories are not Nodes and invariant 102 does not bind them) and the pointer list to the six follow-up walkthroughs (packets 02–07) plus the existing `sonarcloud-organization-setup.md`.
- Accepted ADR-0082 (Canonical Node Standup Procedure): flipped status to Accepted, added the new `## Standup Procedure Invariants` section to `constitution/invariants.md` carrying invariant **102** (D6's node-registration-mandatory rule — ten enumerated items including catalog rows, the five-file context folder, sector row, `repo-to-node.yml` mapping, `.honeydrunk-review.yaml`, `pr.yml` calling `pr-core.yml`, branch protection on `pr-core / core`, and org-secret repo binding — with the bootstrap-PR carve-out and the procedural-enforcement clause noting item 10 is outside the `review` agent's admin scope), flipped the `adrs/README.md` row to Accepted, moved the pre-assigned reservation to the Reservation History table in `constitution/invariant-reservations.md`, and registered the `adr-0082-node-standup` initiative with its eight-packet checklist. The canonical procedure document at `constitution/node-standup.md` and the six follow-up walkthroughs land in subsequent packets in this initiative.
- Expanded ADR-0086 from a one-off Grid Review Runner worker into a portable scheduled agent runner framework. The implementation packets now include reusable job specs and Task Scheduler/host-config boundaries for PR review, post-merge audit, `hive-sync`, Lore sourcing, Lore ingest/compile, and Lore signal review, plus a scheduled-job migration packet and runner-health surfacing through hive-sync/weekly briefing.
- Amended ADR-0086 rollout scope to reuse the existing ADR-0044 review-agent GitHub App, add central managed PR-label normalization with a labels-as-code vocabulary, require dual Codex/Claude review synthesis for substantive high-risk work, and pin Task Scheduler startup/restart behavior for the local worker.
- Archived the ADR-0011 Code Review Pipeline initiative: SonarQube Cloud rollout across all 12 .NET-active Grid repos completed, initial-scan findings (Reliability/Maintainability + Security Hotspots on Communications/Observe) triaged, and the org-level GitHub ruleset for `SonarCloud Code Analysis` is enforced (Active) on the onboarded repos. Moved the initiative entry from `initiatives/active-initiatives.md` to `initiatives/archived-initiatives.md`, marked the roadmap row complete, removed ADR-0011 from `initiatives/proposed-adrs.md`, and reordered `initiatives/current-focus.md` (ADR-0086 promoted to rank #1; refilled rank #15 with ADR-0052 — Cost Governance, Budget Alerts, Kill-Switches — the missing governance ADR that ADR-0079's hard 2026-06-15 per-PR cost ceiling exit criterion composes against).
- Accepted ADR-0086 as the Grid Review Runner transport: the OpenClaw signed-webhook review path is superseded by a GitHub-native label/comment queue plus local pull worker, with ADR-0044 and ADR-0079 amended to point at the new substrate and the rollout initiative registered for Phase A/B/C execution.
- Added the active HoneyDrunk workflow standard under `infrastructure/conventions/workflow-standard.md`, making `HoneyDrunk.Actions` the owner for reusable CI/release/deploy mechanics and limiting consumer repos to triggers, version/environment resolution, and repo-specific metadata.
- Promoted three ADR drafts to canonical numbered ADRs and added one new ADR: ADR-0083 (Sensitive Inventory and External-SaaS Credential Rotation Procedure), ADR-0084 (Discord as the Canonical Operator-Alerts Surface), ADR-0085 (Grid-Wide Documentation Currency Agent `docs-sync`), and ADR-0086 (Pull-Based Local Worker as the Grid Review Runner). ADR-0083, ADR-0084, and ADR-0085 land as Proposed; ADR-0086 is now Accepted. Resolves the 0083 numbering conflict — the pre-existing 2026-05-25 drafts hold prior claim per ADR-0082's depends-on prose; the later-authored docs-sync ADR renumbers to 0085. ADR-0086 supersedes the transport halves of ADR-0044 D1/D5 (signed-webhook → OpenClaw → Codex) and ADR-0079 D2 (Anthropic-native Claude Code post June 15) with a single pull-based local-worker substrate running both Reviewer 3 and Reviewer 4 under local CLIs against subscription auth; the discipline halves of ADR-0044 (twenty-category rubric, context-loading contract, authorship / size / sampling discipline) and ADR-0079 (substantive-PR classifier, Invariant 53 satisfaction, `.claude/agents/review.md` source-of-truth) are preserved verbatim. Updates `adrs/README.md` index, ADR-0044 + ADR-0079 with "Superseded in part by ADR-0086" sections, ADR-0082's `(drafts —)` cross-references, and rewrites internal links in the promoted ADR bodies from draft filenames to canonical ADR filenames.
- Accepted ADR-0012 as the Grid CI/CD control-plane decision: finalized invariants 37-41, registered the rollout initiative, and added the review-agent caller-permissions rule.
- Added the ADR-0012 D7 GitHub Actions failure-notification runbook and linked it from the infrastructure index.
- Extended `catalogs/grid-health.json` to schema 1.1 with per-repo `tracked_workflows` for the ADR-0012 grid-health aggregator.
- Accepted ADR-0031 after all four standup packets closed: moved the Audit standup packets to `completed/`, updated filed packet paths/proposed-ADR tracking, recorded Audit v0.1.0 as published, and documented Auth PR #24 as the first `IAuditLog` emitter through `HoneyDrunk.Audit.Abstractions` only.
- Added Audit constitutional invariants 47 (audit-emission boundary), 48 (downstream Nodes depend only on `HoneyDrunk.Audit.Abstractions`), and 49 (Audit Abstractions contract-shape canary); finalized ADR-0030/ADR-0031 invariant references and substituted the assigned numbers in ADR-0031 follow-up packets.
- Amended Audit ADR/standup scope so `HoneyDrunk.Audit` v0.1.0 explicitly supports both activity/security audit and data-change audit via category/outcome/target/change value types, with redaction guidance before scaffold.
- Reconciled ADR-0010 Phase 1 after Observe PR #3 merge: marked Architecture#35/#36 and Observe#2 complete, moved completed/superseded ADR-0010 packets to `completed/`, removed the stale missing packet-04 manifest entry, closed duplicate AI routing issues superseded by ADR-0016, and refreshed roadmap/initiative tracking for Phase 2 scoping.
- Accepted ADR-0010 Phase 1 in Architecture: registered `honeydrunk-observe` across catalogs, added Observe repo context, finalized Observation invariants 29-30, refreshed sector boundaries, and kept AI routing contract catalog alignment without touching package code.
- Registered ADR-0016 HoneyDrunk.AI architecture prerequisites: corrected the AI contract catalog to D3 (`ICostLedger` instead of stale `IInferenceResult`), replaced retired `Providers.Local` references with `Providers.InMemory`, clarified AI emits telemetry consumed by Pulse without a Pulse runtime dependency, added invariants 44-46 for downstream AI coupling, App Config-sourced AI rates/policies, and the AI contract-shape canary, and dropped invariant 28's stale ADR-0010 Proposed qualifier, and flipped ADR-0016 to Accepted in the ADR index/proposed-ADR tracker.
- Reconciled Architecture catalogs and trackers to the Kernel Adoption Alignment release baseline: Kernel 0.7.0, Transport 0.6.0, Vault 0.5.0, Auth 0.4.0, Web.Rest 0.5.0, Data 0.6.0, Notify 0.3.0, Pulse 0.3.0, and Communications 0.2.0.
- Moved closed Kernel Adoption and consolidation packets from `active/` to `completed/` and updated `filed-packets.json` paths while preserving still-open packet issues in `active/`.
- Added ADR-0033 for the environment-gated deploy-trigger model for deployable Nodes.
- Scoped the Kernel Adoption Alignment initiative with per-repo packets for Kernel, Transport, Vault, Auth, Web.Rest, Data, Vault.Rotation, Notify, Pulse, Communications, and Architecture catalog reconciliation.
- Reconciled Pulse's canonical Node ID from legacy `pulse` to `honeydrunk-pulse` across architecture catalogs and naming conventions.
- Accept ADR-0030 (Grid-Wide Audit Substrate). Register `honeydrunk-audit` Node across nodes.json/relationships.json/contracts.json/grid-health.json/modules.json with four new edges (Audit→Kernel, Audit→Data, Auth→Audit planned, Operator→Audit planned). Add Core-sector Audit row. Mark Operator's `IAuditLog` as relocated to `honeydrunk-audit`. (ADR-0018's pre-existing 2026-05-16 amendment recording the `IAuditLog`/`AuditEntry` relocation and Operator's reclassification to consumer-not-owner was verified unchanged — not modified by this PR.) Create `repos/HoneyDrunk.Audit/` context folder. ADR-0030 flipped Proposed → Accepted; ADR-0031 standup remains Proposed (separate initiative).
- Renamed the legacy initiative sync agent to `hive-sync` and moved the runtime contract from GitHub Actions/Anthropic to OpenClaw scheduled/manual execution.
- Added Hive Sync packet lifecycle handling for `active/` → `completed/` moves and `filed-packets.json` path reconciliation.
- Added Hive Sync non-initiative board tracking via `initiatives/board-items.md` and Hive Sync invariants.
- Added ADR/PDR acceptance reconciliation, `accepts:` packet frontmatter guidance, README Status/Date sync, and `initiatives/proposed-adrs.md`.
- Added Hive Sync drift reporting via `initiatives/drift-report.md`.
