# Architecture Decision Records

Index of all ADRs for the HoneyDrunk Grid.

For product-level strategic decisions, see [PDRs](../pdrs/README.md).

| ID | Title | Status | Date | Sector | Impact |
|----|-------|--------|------|--------|--------|
| [ADR-0001](ADR-0001-node-vs-service.md) | Node vs Service Distinction | Accepted | 2026-03-22 | Core | Clarified Node = library package, Service = deployable process. Split catalogs into `nodes.json` and `services.json`. |
| [ADR-0002](ADR-0002-honeyhub-command-center.md) | Architecture Repo as Agent Command Center | Accepted | 2026-03-22 | Meta | Established this repo as the centralized source of truth for all agentic workflows and cross-repo coordination. |
| [ADR-0003](ADR-0003-honeyhub-control-plane.md) | HoneyHub as Organizational Control Plane | Accepted (Phase 1) | 2026-03-30 | Meta | HoneyHub as a graph-driven orchestration system. Accepted Phase 1 (domain model + knowledge graph API). Phases 2–4 (orchestration, projections, UI) are future commitments. |
| [ADR-0004](ADR-0004-tool-agnostic-agent-definitions.md) | Tool-Agnostic Agent Definitions | Superseded by [ADR-0007](ADR-0007-claude-agents-as-source-of-truth.md) | 2026-04-08 | Meta | Agent definitions authored once in canonical format, generated into tool-specific formats for Claude Code, Copilot, and Codex. |
| [ADR-0005](ADR-0005-configuration-and-secrets-strategy.md) | Configuration and Secrets Strategy | Accepted | 2026-04-09 | Infrastructure | Defined per-Node per-environment Key Vault strategy, bootstrap env vars, App Configuration split, and RBAC/OIDC access model. |
| [ADR-0006](ADR-0006-secret-rotation-and-lifecycle.md) | Secret Rotation and Lifecycle | Accepted | 2026-04-09 | Infrastructure | Defined rotation SLAs, Azure-native and third-party rotation paths, event-driven cache invalidation, and audit/alert requirements. |
| [ADR-0007](ADR-0007-claude-agents-as-source-of-truth.md) | `.claude/agents/` as Single Source of Truth | Accepted | 2026-04-09 | Meta | Dropped canonical/generator layer. Agents live directly in `.claude/agents/`; Copilot discovers them there, Codex uses `AGENTS.md`. |
| [ADR-0008](ADR-0008-work-tracking-and-execution-flow.md) | Work Tracking and Execution Flow | Accepted | 2026-04-09 | Meta | Standardized packet-to-issue-to-board-to-PR lifecycle, org-level project schema, and cloud agent execution flow. **See Unresolved Consequences section for D4/D5/D6 gaps.** |
| [ADR-0009](ADR-0009-package-scanning-policy.md) | Package Scanning Policy | Accepted | 2026-04-09 | Meta | PR gate on High+ CVEs, nightly deep scans, no Dependabot. 8 rollout packets in flight. |
| [ADR-0010](ADR-0010-observation-layer.md) | Observation Layer and AI Routing | Accepted | 2026-04-12 | Ops | Establishes HoneyDrunk.Observe (Ops) with provider-slot connectors, and IModelRouter routing contracts in HoneyDrunk.AI. Phase 1 scoped. Invariants 28–30 live. |
| [ADR-0011](ADR-0011-code-review-and-merge-flow.md) | Code Review and Merge Flow | Accepted | 2026-04-12 | Meta | Tiered PR pipeline (tier-1 gates required; SonarQube Cloud tier-2 on public repos; review agent tier-3). Amended by ADR-0044 (reverses D10 local-only stance via cloud-wired Grid Review Runner). Invariants 31–33 binding. |
| [ADR-0012](ADR-0012-grid-cicd-control-plane.md) | HoneyDrunk.Actions as Grid CI/CD Control Plane | Accepted | 2026-05-25 | Meta | Names HoneyDrunk.Actions as the CI/CD control plane for shared tool config (`.github/config/`), direct-CLI reusable workflows, caller-permissions contract, and grid-health aggregator. Triggered by the 2026-04-13 Vault nightly cascade. Invariants 37–41. |
| [ADR-0013](ADR-0013-communications-orchestration-layer.md) | Communications Orchestration Layer | Superseded by [ADR-0019](ADR-0019-stand-up-honeydrunk-communications-node.md) | 2026-04-16 | Ops | Historical boundary decision for HoneyDrunk.Communications as the decision/orchestration layer above Notify. Superseded by ADR-0019 as the canonical stand-up ADR. |
| [ADR-0014](ADR-0014-hive-architecture-reconciliation-agent.md) | Hive–Architecture Reconciliation Agent | Accepted | 2026-04-16 | Meta | Renames `initiatives-sync` to `hive-sync` with a broader mandate. |
| [ADR-0015](ADR-0015-container-hosting-platform.md) | Container Hosting Platform for Deployable Nodes | Accepted | 2026-04-18 | Infrastructure | Azure Container Apps for containerized Nodes (Notify.Worker, Pulse.Collector). Function Apps remain for Notify.Functions. Adds a reusable Container Apps deploy workflow to HoneyDrunk.Actions; 5-packet rollout. |
| [ADR-0016](ADR-0016-stand-up-honeydrunk-ai-node.md) | Stand Up the HoneyDrunk.AI Node | Accepted | 2026-04-19 | AI | Stand-up decision for AI as the inference substrate. Freezes seven contracts (IChatClient, IEmbeddingGenerator, IModelProvider, IModelRouter, IRoutingPolicy, ModelCapabilityDeclaration, ICostLedger). Abstractions-first coupling rule; contract-shape canary required. |
| [ADR-0017](ADR-0017-stand-up-honeydrunk-capabilities-node.md) | Stand Up the HoneyDrunk.Capabilities Node | Proposed | 2026-04-19 | AI | Stand-up decision for Capabilities as the tool registry and dispatch substrate. Replaces placeholder `ICapability`/`ICapabilityPermission` with four separated contracts (`ICapabilityRegistry`, `CapabilityDescriptor`, `ICapabilityInvoker`, `ICapabilityGuard`). Same Abstractions-first pattern as ADR-0016. |
| [ADR-0018](ADR-0018-stand-up-honeydrunk-operator-node.md) | Stand Up the HoneyDrunk.Operator Node | Proposed | 2026-04-19 | AI | Stand-up decision for Operator as the human-policy enforcement and audit substrate. Freezes `IApprovalGate`, `ICircuitBreaker`, `ICostGuard` (renamed from `ICostController`), `ISafetyFilter`; adds record types for cost/audit/approval. Pins boundaries against Auth and Communications. |
| [ADR-0019](ADR-0019-stand-up-honeydrunk-communications-node.md) | Stand Up the HoneyDrunk.Communications Node | Accepted | 2026-04-19 | Ops | Stand-up decision for Communications as the orchestration substrate above Notify. **Bundles the Notify refactor into acceptance** (removes `INotificationPolicy`, `PolicyEvaluationResult`, `RejectionReason.PolicyDenied`, renames `Orchestration/`→`Intake/`) — deliberate exception to the lean-standup convention for boundary integrity from day one. Adds invariants 40–43. |
| [ADR-0020](ADR-0020-stand-up-honeydrunk-agents-node.md) | Stand Up the HoneyDrunk.Agents Node | Proposed | 2026-04-19 | AI | Stand-up decision for Agents as the agent runtime substrate. First Node composing all three AI-sector substrates (AI + Capabilities + Operator). Freezes `IAgent`, `IAgentExecutionContext`, `IToolInvoker`, `IAgentMemory`; pins function-calling adapter placement in Agents runtime (not AI, not Capabilities). State-boundary rule: Agents holds ephemeral execution state, Memory holds durable learned state, Flow holds durable workflow state. |
| [ADR-0021](ADR-0021-stand-up-honeydrunk-knowledge-node.md) | Stand Up the HoneyDrunk.Knowledge Node | Proposed | 2026-04-19 | AI | Stand-up decision for Knowledge as the external ingestion/retrieval substrate. Four contracts (`IKnowledgeStore`, `IDocumentIngester`, `IRetrievalPipeline`, `KnowledgeSource`). Pins embedding-model coherence: every source records the ingest-time model identifier; mismatched similarity search errors or returns empty. Two-paradigm support (RAG + structured wikis) behind the four interfaces as provider-axis concern. |
| [ADR-0022](ADR-0022-stand-up-honeydrunk-memory-node.md) | Stand Up the HoneyDrunk.Memory Node | Proposed | 2026-04-19 | AI | Stand-up decision for Memory as the agent-memory substrate. Three interfaces (`IMemoryStore`, `IMemoryScope`, `IMemorySummarizer`); short-term state pinned to Agents' execution context (resolves boundaries.md drift). Three-level scope (Tenant/Project/Agent); cross-agent escalation via Auth policy. Memory owns conversation transcripts; Flow owns step state. Retention/PII enforcement in Memory, policy in Operator. |
| [ADR-0023](ADR-0023-stand-up-honeydrunk-evals-node.md) | Stand Up the HoneyDrunk.Evals Node | Proposed | 2026-04-19 | AI | Stand-up decision for Evals as the evaluation/quality substrate. Resolves three-way contract drift; definitive set is four interfaces (`IEvaluator`, `IEvalScorer`, `IEvalSuite`, `IEvalTarget`) + two records (`EvalCase`, `EvalReport`). Pins router-bypass via `IEvalTarget` (resolves ADR-0016 D3 follow-through). Deliberate telemetry-content carve-out — eval signals may carry prompts/outputs unless suite declares sensitivity. |
| [ADR-0024](ADR-0024-stand-up-honeydrunk-flow-node.md) | Stand Up the HoneyDrunk.Flow Node | Proposed | 2026-04-19 | AI | Stand-up decision for Flow as the workflow-orchestration substrate. Five interfaces (`IWorkflowEngine`, `IWorkflow`, `IWorkflowStep`, `IWorkflowState`, `ICompensation`); human-in-the-loop resume via event-out pattern (symmetric with ADR-0018 D8 deferral). Orchestration-based per-step compensation at stand-up; saga primitives deferred. Flow holds coordination state only — no direct Memory edge. |
| [ADR-0025](ADR-0025-stand-up-honeydrunk-sim-node.md) | Stand Up the HoneyDrunk.Sim Node | Proposed | 2026-04-19 | AI | Stand-up decision for Sim as the simulation/plan-evaluation substrate. Closes the AI-sector stand-up wave (9th of 9). Two interfaces (`ISimulator`, `IPlanValidator`) + `ISimulationTarget` (router-bypass parallel to Evals' `IEvalTarget`) + three records (`Scenario`, `RiskAssessment`, `SimulationResult`). Evals-vs-Sim boundary: side-effect-freedom. Sim composes Operator primitives (`ICostGuard`/`ISafetyFilter`/`IApprovalGate`) to predict real-execution policy outcomes. |
| [ADR-0026](ADR-0026-grid-multi-tenant-primitives.md) | Grid Multi-Tenant Primitives | Accepted | 2026-05-02 | Core / Ops / Infrastructure | Formalizes `TenantId` (with `Internal` sentinel), `ITenantRateLimitPolicy`, `IBillingEventEmitter`, and the per-tenant Vault scoping convention as **Grid-wide primitives in Kernel.Abstractions and Vault**, not Notify-specific. Notify Cloud (PDR-0002) is the first consumer. Promotes `IGridContext.TenantId` from `string?` to `TenantId?`. Adds a boundary invariant: tenant concerns live in gateway-layer middleware and post-dispatch tails, never in core dispatch paths — turns PDR-0002 §K kill condition 2 into a Grid-level rule. |
| [ADR-0027](ADR-0027-stand-up-honeydrunk-notify-cloud-node.md) | Stand Up the HoneyDrunk.Notify.Cloud Node | Proposed | 2026-05-02 | Ops | Stand-up decision for Notify Cloud as the multi-tenant commercial wrapper above Notify and Communications — first commercial Node on the Grid (PDR-0002). **First private repo in the Grid** (revenue carve-out from public-by-default, justified in D2). Two frozen interfaces (`INotifyCloudGateway`, `INotifyCloudApiKeyStore`) + two frozen records (`NotifyCloudTenantTier`, `ApiKeyIssuance`); the multi-tenant primitives (`TenantId`, `ITenantRateLimitPolicy`, `IBillingEventEmitter`, `BillingEvent`) are consumed from Kernel per ADR-0026, not redefined here. Picks **FSL** for the open engine repos (`HoneyDrunk.Notify`, `HoneyDrunk.Communications`). SDK stays in the open Notify repo. Hard prerequisite: ADR-0026. |
| [ADR-0028](ADR-0028-event-driven-architecture-and-messaging.md) | Event-Driven Architecture and Messaging | Proposed | 2026-05-04 | Core / Ops / cross-cutting | Use-case-first matrix mapping the Grid's eight event-driven shapes (cross-Node async commands, pub/sub fanout, commodity work queues, telemetry, cron, reactive resource events, in-process events, dead-letter handling) to six backings (Service Bus, Storage Queue, Event Grid, OTLP, GitHub Actions cron, in-process). Names Service Bus as default broker with one shared namespace per environment; defers Event Grid custom topics; pins Pulse signals as not domain events. |
| [ADR-0029](ADR-0029-cloudflare-dns-and-edge-platform.md) | Cloudflare as Registrar, Authoritative DNS, and Edge Platform | Proposed | 2026-05-08 | Infrastructure | Commits Cloudflare as the Grid's registrar (transfer from GoDaddy), authoritative DNS (portal-managed at v1; IaC deferred), and edge platform of choice. Conservative in scope: Workers, Pages, Tunnel, Access, R2, and Cloudflare for SaaS each get their own follow-up ADRs. Migration order is Studios marketing site first, then Node-bound subdomains. |
| [ADR-0030](ADR-0030-grid-wide-audit-substrate.md) | Grid-Wide Audit Substrate | Accepted | 2026-05-16 | Core | Establishes durable, attributable security/action audit as a first-class Grid concern homed in a new dedicated `HoneyDrunk.Audit` Node — Operator (recorder ≠ actor) and Kernel (domain concern, not a primitive) both rejected as homes. Relocates `IAuditLog`/`AuditEntry` out of Operator, generalizes them Grid-wide, adds `IAuditQuery`. Phased-integrity scope: Phase 1 is append-only-by-interface (explicitly **not** tamper-evident); hash-chain/WORM and the tenant-facing forensics Service deferred behind the now-existing boundary. Amends ADR-0018. Standup governed by ADR-0031. |
| [ADR-0031](ADR-0031-stand-up-honeydrunk-audit-node.md) | Stand Up the HoneyDrunk.Audit Node | Accepted | 2026-05-16 | Core | Stand-up decision for the `HoneyDrunk.Audit` Node driven by ADR-0030. `HoneyDrunk.Audit.Abstractions` (`IAuditLog`, `IAuditQuery`, `AuditEntry`) + Data-backed `HoneyDrunk.Audit.Data` impl (naming follows the `HoneyDrunk.Data.*` backing-slot precedent). Auth is first emitter; Operator reclassified consumer-not-owner. New edges: Audit→Data, Audit→Kernel, Auth→Audit, Operator→Audit. Same Abstractions-first coupling rule + contract-shape canary as the AI-sector stand-ups. |
| [ADR-0032](ADR-0032-pr-validation-policy-coverage-gate-and-nuget-flagging.md) | Grid-Wide PR Validation Policy — Coverage Gate and NuGet Update Flagging | Proposed | 2026-05-17 | Meta | Adds a **blocking coverage gate** plus **non-blocking NuGet update flagging** to `pr-core.yml` in HoneyDrunk.Actions, owned by the Grid CI/CD control plane per ADR-0012 and consumed by every Node via `workflow_call`. Coverage gate fails the PR if any of three conditions are violated: **patch coverage ≥ 75%** on changed/added lines (tunable input `patch-coverage-threshold`), **no-regress** against a committed `.github/coverage-baseline.json` baseline file (post-merge step ratchets the baseline up), and **absolute floor ≥ 70%** total solution coverage (flat Grid-wide). Solutions without test projects skip the gate entirely (deliberate — does not punish repos that have no tests yet). Outdated NuGet packages **never block the build** per ADR-0009; flagging is surfaced via a durable per-repo `.github/nuget-status.md` artifact analogous to ADR-0012 D6's grid-health visibility pattern. Reads coverage from a committed baseline file (not `gh api` / artifacts) — same reasoning as ADR-0012 D2's committed shared config over release-asset URLs. |
| [ADR-0033](ADR-0033-environment-gated-deploy-trigger-model.md) | Environment-Gated Deploy-Trigger Model for Deployable Nodes | Proposed | 2026-05-18 | Ops | Hybrid trigger model for the three deployable lines (Notify.Functions, Notify.Worker, Pulse.Collector): path-filtered push-to-`main` deploys `dev` continuously; SemVer tags gate `staging`/`prod`. Change lives entirely in consumer release workflows — **no HoneyDrunk.Actions change**, ADR-0012 invariant untouched. Resolves ADR-0015's deferred staging/prod gating and records the rebuild-from-tag promotion model. Mandates per-deployable path scoping (multi-deployable repos) and environment-keyed concurrency. No new invariants, no relationships cascade. |
| [ADR-0034](ADR-0034-public-package-distribution-and-nuget-policy.md) | Public Package Distribution and NuGet Policy | Proposed | 2026-05-21 | Meta / cross-cutting | nuget.org under `HoneyDrunkStudios` as the primary public feed; GitHub Packages for private revenue Nodes; Azure Artifacts retained as pre-release staging. Mandates required `Directory.Build.props` metadata, SourceLink + `.snupkg`, deterministic builds, author signing (gated on BDR-0001 entity finalization), and a single `job-publish-nuget.yml` reusable workflow in HoneyDrunk.Actions. Approved-feeds list lives in `catalogs/package-feeds.json`. Lands together with ADR-0035. |
| [ADR-0035](ADR-0035-abstractions-versioning-and-deprecation-policy.md) | Abstractions Versioning and Deprecation Policy | Proposed | 2026-05-21 | Meta / cross-cutting | Strict SemVer on every public Abstractions package with binary-compat guarantee at minor/patch; no default-interface-member additions (new behavior lands on new interfaces); records use `init` members not positional syntax; `-preview.N` minimum 14 days before stable and 60-day deprecation window for member removals. Cascade ABI bumps are initiatives, not loose packets. Enforces via `Microsoft.CodeAnalysis.PublicApiAnalyzers` + an API-diff job in HoneyDrunk.Actions. Lands together with ADR-0034. |
| [ADR-0036](ADR-0036-disaster-recovery-and-backup-policy.md) | Disaster Recovery and Backup Policy | Proposed | 2026-05-21 | Infrastructure / cross-cutting | Three tiers (T0/T1/T2) with explicit RPO/RTO targets, geo-posture, and restore-drill cadence. Vault, Audit, and Notify Cloud tenant identity/billing data are T0. Manual cross-region failover with documented runbooks (no automated failover at solo-developer scale). Vault has a bootstrap-recovery procedure that must exist before any other T0 drill. Adds `dr_tier` field to `catalogs/grid-health.json`; missed drills freeze tier-affecting tenant onboarding. |
| [ADR-0037](ADR-0037-payment-and-billing-integration.md) | Payment and Billing Integration | Proposed | 2026-05-21 | Ops / cross-cutting | Stripe (Billing + Tax + Checkout + Customer Portal) as the sole processor for all products, B2B and B2C; no Stripe Connect. Introduces a future `HoneyDrunk.Billing` Node owning the `IBillingEventEmitter` → buffer → Stripe Meter Events pipe, idempotent end-to-end per ADR-0042. Single webhook handler per environment in `HoneyDrunk.Billing.Webhooks`; domain events flow on Service Bus per ADR-0028. Card data never enters the Grid (PCI SAQ A scope). Mobile in-app purchase carve-out for app-store-mandated flows. |
| [ADR-0038](ADR-0038-outbound-sender-identity-and-deliverability.md) | Outbound Sender Identity and Deliverability | Proposed | 2026-05-21 | Ops | Subdomain split for reputation isolation (`mail.` / `notify.` / tenant-delegated DKIM); full SPF + DKIM + DMARC at staged-strict policy on every sending subdomain. 10DLC for US tenant SMS + a Studio toll-free number for transactional. Single ESP primary + cold fallback with subaccount-per-tenant. New `IDeliverabilityFeedbackSink` in Notify.Abstractions; suppression is per-tenant with a platform-wide override list. RFC 8058 one-click List-Unsubscribe mandatory on bulk and platform sends. |
| [ADR-0039](ADR-0039-grid-open-source-license-policy.md) | Grid Open Source License Policy | Proposed | 2026-05-21 | Meta / cross-cutting | MIT as the Grid default; FSL-1.1-MIT for revenue Nodes (formalizes the ADR-0027 precedent); MIT for SDKs even when the engine is FSL; proprietary for private Nodes. DCO (not CLA) for contributions; no per-file license headers; CC-BY-4.0 for documentation/content. License field added to `catalogs/nodes.json`; `hive-sync` reconciles. License changes are an ADR amendment with a one-minor-release `LICENSE.next` window. |
| [ADR-0040](ADR-0040-telemetry-backend-and-retention.md) | Telemetry Backend and Retention | Proposed | 2026-05-21 | Ops / cross-cutting | **Azure Monitor + Application Insights** as the unified backend for traces, metrics, and logs — leverages the existing Azure relationship (ADR-0015) rather than adopting a new vendor. HoneyDrunk.Observe stays the OTLP-only boundary via the Azure Monitor OpenTelemetry Distro; backend reversibility preserved. Retention: traces 90d, metrics 93d, logs 90d standard / **730d for Audit-sourced logs** (longer than Grafana Cloud free tier's 14d metrics). Adaptive sampling via App Insights SDK with 100% rules for errors/Audit/billing/canaries; cardinality discipline via volume-not-series billing model. Native cross-signal navigation via `operation_id` (no configured cross-links needed). Application Map is the "see the whole Grid at once" surface. Realistic v1 cost ~$0–30/month; ceiling $100/month. **Grafana Cloud + Sentry documented as the D11 escalation path** with explicit triggers (signal-by-signal, evidence-based escalation only). |
| [ADR-0041](ADR-0041-ai-model-registry-and-approval-workflow.md) | AI Model Registry and Approval Workflow | Proposed | 2026-05-21 | AI | Declarative `models.json` in HoneyDrunk.AI exposed via new `IModelRegistry` + `ModelRegistration` (capability declaration, cost profile with per-call ceiling, approval state, routing hints). Initial approved providers: Anthropic, OpenAI, Azure OpenAI. Adding a model is a packet with a 14-day `Preview` window; adding a *provider* is an ADR amendment. Nightly capability canary auto-flips approval state. Cost-aware default routing policy; per-call ceiling sits in front of ADR-0018's per-tenant `ICostGuard`. Every dispatch emits Audit per ADR-0030. |
| [ADR-0042](ADR-0042-idempotency-contract-for-async-boundaries.md) | Idempotency Contract for Async Boundaries | Proposed | 2026-05-21 | Core / cross-cutting | Mandatory `IdempotencyKey` on every async domain-event message (Pulse signals and telemetry exempt). Adds `IGridMessageEnvelope.IdempotencyKey`, `IIdempotencyStore`, and `IdempotentMessageHandler<T>` to Kernel.Abstractions. Dedup state per consumer-group with Cosmos default backing; 7-day TTL standard, 30-day for billing/audit. Downstream message keys deterministically derived (`SHA256(inbound:relationship)`) for end-to-end idempotency. Hard prerequisite for ADR-0037; load-bearing for ADR-0036's T2 failover claim. |
| [ADR-0043](ADR-0043-continuous-backlog-generation-strategy.md) | Continuous Backlog Generation Strategy | Proposed | 2026-05-21 | Meta | Codifies four agent-driven backlog sources (Strategic via `scope` on ADR/PDR acceptance, Tactical via rotating `node-audit`, Opportunistic via monthly `product-strategist` Scout, Reactive via `hive-sync` drift/CVE/incident detection) plus a weekly `netrunner` briefing as the single human-AI triage surface. Adds the `proposed/` → `active/` → `completed/` packet lifecycle with `proposed/` as the agent-output landing zone — agents never self-promote. Defers the execution surface (OpenClaw vs GitHub Actions vs Claude Code `/loop` vs manual) to a 90-day follow-up ADR; until then, the cadence is honored manually. Staged 4-phase rollout. |
| [ADR-0044](ADR-0044-grid-aware-cloud-code-review-and-ai-authored-pr-discipline.md) | Grid-Aware Cloud Code Review and AI-Authored PR Discipline | Accepted | 2026-05-21 | Meta | Builds an in-house cloud-wired Grid-aware code reviewer (`job-review-request.yml` in HoneyDrunk.Actions) that runs the existing `.claude/agents/review.md` on every non-draft PR, posts an advisory comment, and loads Grid context actively (catalogs, invariants, packet via PR-body link). Pins a **twenty-category review rubric** as ADR-bound (correctness, architectural integrity, maintainability, reuse, SOLID, performance, reliability, observability, security, enterprise readiness, testing, API/contract, data integrity, distributed systems, CI/CD, DX, business alignment, AI/agent-specific, anti-entropy, human factors). **Upstream-awareness clause** — the rubric binds authoring agents (`scope`, `adr-composer`, `pdr-composer`, `refine`, `node-audit`, execution surfaces) at authoring time, not only the reviewer at evaluation time. Reverses ADR-0011 D10's local-only posture and D11's CodeRabbit rejection. Adds AI-authored PR discipline: mandatory `Authorship:` declaration, PR-size limits (400/800 lines), high-risk-Node multi-perspective requirement, post-merge sampling audit. Phased 4-step rollout. Uses the subscription-backed OpenClaw/Codex runner by default; external API-backed review remains a future opt-in fallback. Superseded in part by ADR-0086 (transport + default runtime substrate). |
| [ADR-0045](ADR-0045-grid-wide-error-tracking.md) | Grid-Wide Error Tracking | Proposed | 2026-05-21 | Ops / cross-cutting | Adds **errors** as a fourth Grid signal type (alongside traces/metrics/logs from ADR-0040). v1 backend: **Application Insights' Failures + exception tracking** (same App Insights resource per ADR-0040 — no new vendor, no new billing line). Errors flow through HoneyDrunk.Observe via a new `IErrorReporter` abstraction backed by the App Insights .NET SDK (provider-slot pattern, preserves reversibility). Native cross-signal navigation via `operation_id` — click an error → see the trace → see logs → see metrics, no configured cross-links. **Migrates Notify off its existing Sentry one-off** onto App Insights (parallel-output window, cutover, archive Sentry account). Release tracking via App Insights' `application_Version` and release annotations API. Acknowledges App Insights' Failures-blade as "Sentry-lite" (real but rougher); **Sentry documented as the D11 escalation path** with explicit triggers (error-triage volume, release workflow polish, multi-tenant filtering, AI breadcrumb depth). v1 cost near-zero on the error path; budget rolls up into ADR-0040's $100/month ceiling. |
| [ADR-0046](ADR-0046-specialist-review-agents.md) | Specialist Review Agents | Proposed | 2026-05-21 | Meta | Layers **specialist agents** on top of the ADR-0044 generalist `review` agent — narrower scope, deeper rigor per lens, invoked selectively. Initial roster of five: **`cfo`** (cost/vendor discipline **including AI-cost** — model selection, token budgets, prompt-caching opportunities; retroactively justifies itself against the ADR-0040/0045 first-draft cost overcommit), **`a11y`** (WCAG/accessibility for UI work), **`security`** (OWASP/threat modeling for Auth/Vault/tenant/public-API touches), **`performance`** (runtime/scalability — allocations, N+1, hot-path discipline, concurrency safety for deployable Nodes), **`ai-safety`** (prompt injection, tool scoping, agent guardrails for AI-sector Nodes). AI-cost folded into `cfo` rather than carved out — same analytical framework. **Manual invocation at v1** — no CI triggers, no automatic firing; the operator decides when a lens applies. **Specialists are upstream-aware** per ADR-0044 D3 — the highest-leverage invocation is at ADR/PDR/packet drafting, not at PR review. Phased rollout: `cfo` → `security` → `performance` → `ai-safety` → `a11y`. CI-triggered invocation, output-format standardization, and roster expansion (privacy, SRE, migration safety, anti-entropy, DX) explicitly deferred. |
| [ADR-0047](ADR-0047-testing-patterns-and-tooling.md) | Testing Patterns and Tooling | Accepted | 2026-05-21 | Meta / cross-cutting | Codifies the testing pyramid (unit / integration-in-process / integration-with-containers / E2E) plus canaries (per Invariant 14). **Unit:** xUnit + **NSubstitute** (not Moq — 2023 SponsorLink stewardship incident) + **AwesomeAssertions** (MIT fork of FluentAssertions v7, chosen over the now-paid FluentAssertions v8) + coverlet; coverage thresholds per ADR-0036 DR tier (T0 85/80, T1 75/70, T2 60/55). **Integration:** WebApplicationFactory + in-process Invariant 15 fakes (Tier 2a) split from Testcontainers-driven real-dependency tests (Tier 2b). **E2E web:** Playwright **.NET binding** (one language across the stack). **E2E mobile: Maestro** — declarative YAML, cross-platform, survives the still-pending mobile-platform decision. **Test data:** AutoFixture + hand-written Builders + Bogus where realistic seed data matters. **Performance/load:** BenchmarkDotNet for micro-benchmarks (manual-only at v1) + Azure Load Testing for macro load testing (sunk-cost Azure vendor per ADR-0040 pattern). Closes ADR-0011 Gap 1 (integration) and Gap 3 (E2E). Phased 6-step rollout — unit stack with Moq→NSubstitute + FluentAssertions→AwesomeAssertions migrations → Tier 2a → Tier 2b → E2E web → E2E mobile → ongoing perf. |
| [ADR-0048](ADR-0048-data-schema-evolution-and-migration-policy.md) | Data Schema Evolution and Migration Policy | Proposed | 2026-05-22 | Core / cross-cutting | EF Core Migrations as the Grid-wide framework with strict **expand → migrate code → contract** for Tier 0/Tier 1 relational stores; Cosmos and other document stores follow schema-on-read with backfill-by-runbook. Migrations run **out-of-band** via a new `migrate.yml` reusable workflow in HoneyDrunk.Actions, executed before the dependent code deploy — explicitly rejects startup-time `Database.Migrate()` and init-containers because both race ADR-0015 D6's multi-revision traffic-split window. Forward-only by default; mandatory `[Rollback]` declaration + Tier 2b round-trip test per ADR-0047. Per-tier backward-compatibility windows (Audit indefinite per ADR-0030 D8, customer-facing 14 days / 2 deploys, idempotency stores 30 days matching ADR-0042 D4). New specialist `database` review agent (per ADR-0046 pattern); explicit append-only-by-interface constraints on the `AuditEntry` table. |
| [ADR-0049](ADR-0049-data-classification-pii-handling-and-retention-schedule.md) | Data Classification, PII Handling, and Retention Schedule | Proposed | 2026-05-22 | Core / cross-cutting | Closes Invariant 47's undefined "sensitive field" — commits a four-tier classification taxonomy (Public / Internal / Confidential / Restricted) plus a PII sub-taxonomy (PII / Sensitive PII / Pseudonymous) with explicit GDPR Art. 9 mapping. Mechanical enforcement via `[Classification]` and `[PiiField]` attributes in `HoneyDrunk.Kernel.Abstractions` + a `HoneyDrunk.Analyzers` CI rule. Grid-wide retention schedule consolidates ADR-0040 telemetry (90/93/730d), ADR-0030 audit (T0 = 7yr extension), ADR-0036 backup, ADR-0045 errors (90d). Resolves ADR-0036 D7's deferred backup-vs-erasure question via pseudonymous-token-in-audit + erasable PII map (operational mechanics handed to ADR-0050). Azure US East 2 + SCCs + DPF as v1 data-residency posture. Adds `catalogs/data-classification.json`; promotes the `database` specialist to v1 roster. |
| [ADR-0050](ADR-0050-tenant-lifecycle-provisioning-suspension-offboarding-and-data-export.md) | Tenant Lifecycle: Provisioning, Suspension, Offboarding, and Data Export | Proposed | 2026-05-22 | Core / cross-cutting | Seven-state lifecycle (`Prospect → Trialing → Active → PastDue → Suspended → Offboarding → Closed`) with explicit transition initiators; **self-serve-with-manual-approval** provisioning for Notify.Cloud v1; idempotent 9-step provisioning (Auth + Vault + Data + Notify + Billing + Communications + Audit) per ADR-0042; lifecycle hosted in HoneyDrunk.Communications per ADR-0019. 67-day default total grace window from payment failure → permanent deletion. **Resolves the GDPR Art. 17 right-to-erasure vs Invariant 47 append-only collision** (D6) via pseudonymization-at-the-audit-boundary: audit substrate stores only opaque `pt_*`/`pu_*` CSPRNG tokens; PII↔token map lives in an erasable `HoneyDrunk.Auth.IdentityMap` table; erasure deletes the map row, audit tokens become permanently unresolvable — GDPR-compliant under Art. 4(5) + EDPB guidance, Invariant 47 fully preserved. Self-serve JSON+CSV ZIP export via 7-day signed Blob URL. Unblocks ADR-0027, ADR-0037, PDR-0002. |
| [ADR-0051](ADR-0051-ai-agent-authorization-and-tool-scoping-model.md) | AI Agent Authorization and Tool Scoping Model | Proposed | 2026-05-22 | AI / cross-cutting | Three-principal model (`UserPrincipal` / `ServicePrincipal` / new `AgentPrincipal`) with versioned **capability bundles** as the grant unit, enforced at the `IToolInvoker` boundary; **intersection-not-union delegation** prevents agents from exceeding either their bundle or their `OnBehalfOf` principal's permissions — closes the load-bearing gap in ADR-0017/0018/0020 and extends ADR-0006's `IAuthorizationPolicy` to non-human principals while preserving Invariant 10. Tenant-scoped capabilities resolved against `RequestContext` per ADR-0026. Operator-self-grant recursion structurally prevented via bundle-name whitelist on `bundle:grid-operator` + startup-time recursion check at the Capabilities Node; dev-only `bundle:local-dev-all` env-guarded — Capabilities refuses to boot if loaded in staging/prod per ADR-0033. v1 stores bundles as YAML in `HoneyDrunk.Capabilities/policies/` (PR-reviewed); OPA + dynamic store + per-tenant bundles as documented v2 paths. Deny-by-default with typed `AuthorizationDeniedException`; every authz decision audited per ADR-0030, fail-closed if Audit unavailable. |
| [ADR-0052](ADR-0052-cost-governance-budget-alerts-and-kill-switches.md) | Cost Governance, Budget Alerts, and Kill-Switches | Accepted | 2026-05-22 | Ops / cross-cutting | Five-category cost model (Azure infra, AI inference, third-party SaaS, domain/cert/registrar, GitHub Actions) with per-category soft/hard caps and **layered enforcement**: in-process `ILlmDispatcher` kill-switch for AI inference via `BudgetExceededException`, Azure Cost Management-driven Container Apps auto-suspend for non-production infra, GitHub-native spending limit for CI, soft-cap alerting only for SaaS/registrar. 50/75/90/100% threshold pings + daily roll-up + hard-cap dual-channel alerts via Communications + Notify. `ICostLedger` in `HoneyDrunk.Kernel` (abstraction only), v1 implementation co-located in `HoneyDrunk.AI` (Cosmos-backed, in-memory cache, write-behind, 13-month retention). Per-tenant attribution (D5, upstream of ADR-0037) + per-agent/per-run attribution (D6, upstream of ADR-0051). Anomaly detection (5x hour-over-hour, 3x day-over-day) via App Insights alert rules per ADR-0040 catches runaway burn before threshold caps fire. Time-bounded Operator override (24h default), audited per ADR-0030. Dev environments get separate smaller caps on resource-isolated subscriptions. Seven-phase rollout. |
| [ADR-0053](ADR-0053-environments-branching-and-release-cadence.md) | Environments, Branching, and Release Cadence | Proposed | 2026-05-22 | Meta / cross-cutting | Three-environment topology — `dev` (always-on, auto from `main`), `staging` (tag-gated, prod-equivalent), `prod` (tag-gated + self-approval); no per-PR ephemerals at v1 (cost). Single Azure subscription with env-level RBAC at v1; subscription split deferred to first-paying-tenant trigger per PDR-0002 / ADR-0050. **Trunk-based** branching with `{type}/{ticket-or-slug}` convention codifying AI-authored prefixes (`codex/`, `copilot/`, `claude/{agent-slug}-{token}`); 5-day soft branch lifetime + PR-stale-after-7-day alert + 30-day auto-close. **Squash merge** to main for feature work, **merge commits** for release branches. Per-Node release-as-needed cadence (soft target: at least one prod release per month per Live Node, or explicit "no changes" log entry). Local-dev parity via in-memory or Testcontainers stacks per ADR-0047. **Production data MUST NEVER copy to dev or staging** — staging gets synthetic fixtures (cross-ref ADR-0049). Unblocks Notify/Pulse Azure bring-up. |
| [ADR-0054](ADR-0054-incident-response-and-on-call-model-for-a-one-person-studio.md) | Incident Response and On-Call Model for a One-Person Studio | Proposed | 2026-05-22 | Ops / cross-cutting | Honest one-person on-call model. Four-level severity taxonomy scoped to customer impact; **09:00–21:00 ET 7-day coverage** for paying tenants with best-effort outside hours **published in Notify Cloud's SLA** — hard blocker for any tighter SLA until D12's second-human trigger fires. Redundant paging: PagerDuty Starter (~$21–25/mo) + Notify Communications channel (because Notify-itself-down is a credible failure mode). D4 routing table maps every alert source from ADR-0040 / 0045 / 0052 / 0012 / 0030 / 0037 to a default SEV. Single-page rule with 1-hour fingerprint dedup. Seven-state lifecycle (`Open → Acknowledged → Investigating → Mitigating → Resolved → Reviewing → Closed`) with machine-readable post-mortem template for MTTA/MTTR tracking. **Blameless post-mortems within 5 business days** for every SEV-1/SEV-2. New `repos/{node}/runbooks/` Grid-wide convention consumed by Operator (ADR-0018). Quarterly game days pair with ADR-0036 DR drills (first 30 days post-acceptance). |
| [ADR-0055](ADR-0055-feature-flag-and-progressive-rollout-strategy.md) | Feature Flag and Progressive Rollout Strategy | Proposed | 2026-05-22 | Core / cross-cutting | **Azure App Configuration**'s feature-flags surface as the v1 backend (no new vendor — same cost-aware pattern as ADR-0040 / 0045 / 0047). `IFeatureGate` in `HoneyDrunk.Kernel.Abstractions` with concrete implementation in a new `HoneyDrunk.FeatureFlags` Node following the Abstractions/sibling-implementation split. **Three flag categories** with distinct lifecycle policies — **release** (hard 90-day `ExpiresOn` enforced by CI, expired flags fail the build), **permission** (annual review, no expiry), **operational** (kill-switches, annual review, no expiry). `{category}.{node}.{feature}` naming enforced by a new `job-featureflags-validate.yml` workflow + Roslyn analyzer catching undeclared-flag and dead-flag rot. **Anti-patterns named** (no flags as authorization stand-ins per ADR-0051, no `RequestContext` bypass, no string-concatenated flag names, no UX-only permission flags). Permission/operational flips audited per ADR-0030; hot-path flag evaluations sampled at 1% per ADR-0040. LaunchDarkly / GrowthBook documented as D15 escalation paths with explicit triggers. |
| [ADR-0056](ADR-0056-threat-model-and-security-review-cadence.md) | Threat Model and Security Review Cadence | Proposed | 2026-05-22 | Meta / Security / cross-cutting | **STRIDE per trust boundary + AI-overlay threat categories** (prompt injection / indirect prompt injection / model jailbreak / tool poisoning / training-data poisoning / hallucinated-fact downstream impact) grounded in OWASP LLM Top 10 + NIST AI RMF + MITRE ATLAS. Ten-boundary trust inventory (public internet ↔ Web.Rest, tenant ↔ tenant, human ↔ agent, agent ↔ tool, agent ↔ external LLM, studios-internal ↔ paying-tenant). Living artifact at `constitution/threat-model.md` (file authored by follow-up issue). Four-layer review cadence (per-PR `security` specialist from ADR-0046 + quarterly operator-led + per-new-Node entry gate + per-major-dependency-upgrade). Three-layer pentest posture: external pentest **before Notify.Cloud GA** + annual thereafter + quarterly DIY adversarial probing. Dependabot Grid-wide + SBOM commitment + GitHub secret scanning + immediate-rotation runbook per ADR-0006. Eight AI-specific protections including structural prompt-injection defense and explicit untrusted-content wrapping. SEV-1-by-default incident coupling per ADR-0054; 90-day responsible disclosure. SOC 2 / ISO 27001 explicitly deferred. |
| [ADR-0057](ADR-0057-public-http-api-versioning-and-client-sdk-strategy.md) | Public HTTP API Versioning and Client SDK Strategy | Proposed | 2026-05-22 | Core / cross-cutting | **REST + URL-path-prefix versioning** (`/v1/...`, `/v2/...`) as the Grid-wide default; **HoneyHub-only GraphQL schema evolution** as the narrow carve-out — settles ADR-0003's open question. Major version per API *surface*, not per endpoint; at most two majors live concurrently. OpenAPI 3.1 as source-of-truth driving auto-generated TypeScript / Swift / Kotlin SDKs (`@honeydrunk/{api}-sdk-*`), docs at `docs.{api}.honeydrunkstudios.com`, and a breaking-change CI gate per ADR-0011 / 0032. Standardized envelopes: RFC 7807 Problem Details errors carrying `traceId`/`correlationId` per ADR-0040 / 0045, cursor pagination, per-tenant `X-RateLimit-*` headers, `Idempotency-Key` per ADR-0042 (required for billing endpoints). Auth: Bearer API key (machine-to-machine) or OAuth 2.1 with PKCE (user-facing) per ADR-0006. **180-day minimum deprecation window** (RFC 8594 `Deprecation` + `Sunset` headers) + 90/30/7-day tenant notifications via ADR-0019 Communications, audited per ADR-0030. Phased rollout: Web.Rest v1 freeze → Notify v1 → Notify Cloud GA → HoneyHub schema gate. |
| [ADR-0058](ADR-0058-grid-wide-caching-strategy.md) | Grid-Wide Caching Strategy | Proposed | 2026-05-23 | Core / cross-cutting | Commits a Grid-wide caching contract: `ICacheStore<T>` ships in **`HoneyDrunk.Kernel.Abstractions`** (not on any Node's Abstractions surface — caching is opaque across Node boundaries per Invariant 3), with an `InMemoryCacheStore<T>` reference implementation in `HoneyDrunk.Kernel`. Distributed backings (Redis-class, Cosmos-with-TTL, Postgres-with-TTL) are per-Node choices that activate when the consuming workload demands them; the Cache Node ([ADR-0059](./ADR-0059-stand-up-honeydrunk-cache-node.md)) hosts those backings. **Three invalidation lanes**: TTL expiry (default), event-driven via Service Bus per ADR-0028, and tag-based bulk eviction (`RemoveByTagAsync`). Tenant-key isolation invariant: every cached value scoped to `TenantId` per ADR-0026 — no Node may share a cache key across tenant boundaries. Data-classification inheritance per ADR-0049: cached values inherit the source data's classification, so Restricted-tier values cannot be cached in InMemory at multi-tenant boundaries. Out of scope: HTTP/output caching, specific provider selection beyond InMemory reference. Acceptance is paired with ADR-0059. |
| [ADR-0059](ADR-0059-stand-up-honeydrunk-cache-node.md) | Stand Up the HoneyDrunk.Cache Node — Home for Distributed Cache Backings | Proposed | 2026-05-23 | Core | Stand-up decision for `HoneyDrunk.Cache` as the Core sector's home for **distributed-cache backing implementations** of the `ICacheStore<T>` contract committed in [ADR-0058](./ADR-0058-grid-wide-caching-strategy.md) D2. Cache is a **backing host, not a contract owner** — it declares no new abstractions; it implements an abstraction declared in `HoneyDrunk.Kernel.Abstractions`. Front-loaded standup (paired with ADR-0058) — no current Grid consumer pulls on a distributed cache, but the Node is stood up now per the established AI-sector / Audit precedent so feature work doesn't bundle substrate work. **First backing not part of standup scope** — Redis-class / Cosmos-with-TTL / Postgres-with-TTL adapters land later as separate feature packets when the first consumer (likely Notify Cloud multi-replica per ADR-0027, or Communications preference cache per ADR-0019) activates them. Scaffold packet ships solution structure, `HoneyDrunk.Standards` wiring, CI pipeline; **no implementations on day one**. Edges: consumes `honeydrunk-kernel`; no `consumed_by` at stand-up. |
| [ADR-0060](ADR-0060-stand-up-honeydrunk-identity-node.md) | Stand Up the HoneyDrunk.Identity Node — User Record, Credential Seam, and Erasure Fan-Out | Proposed | 2026-05-23 | Core | **Resolves the "Auth is not Identity" confusion** — Invariant 10 ("Auth is validation, never issuance") survives intact; the new `HoneyDrunk.Identity` Node owns what Auth was never meant to: the **user record**, **credential storage** (passwords / passkeys / OAuth refresh tokens via wrapped external IdP), **internal-token issuance** (signup / login / refresh), and the **account-deletion fan-out** for ADR-0050 D6's user-level GDPR Art. 17 path. Five frozen contracts: `IUserDirectory`, `IUserProfileStore`, `IInternalTokenIssuer`, `IExternalIdpClaimMapper`, `IIdentityDeletionFanout` + supporting records (`UserId`, `PrincipalId`, `ExternalSubject`, `UserProfile`, `InternalToken`, `DeletionIntent`, `DeletionAck`). The `UserId` ↔ `PseudoUserToken` map **relocates** from `HoneyDrunk.Auth.IdentityMap` (interim home per ADR-0050) to `HoneyDrunk.Identity.IdentityMap` (additive amendment — ADR-0050 D6 architectural posture unchanged). Driven by PDR-0005 Hearth (scout's first-build pick) needing signup, with PDR-0003 Lately and PDR-0008 Curiosities queued behind. Entra adapter is **not** bundled into standup; lands as first feature packet when Hearth or Lately pulls on it. Vendor confirmation deferred (later filled by [ADR-0078](./ADR-0078-end-user-identity-entra-external-id.md)). |
| [ADR-0061](ADR-0061-stand-up-honeydrunk-files-node.md) | Stand Up the HoneyDrunk.Files Node — Blob Storage, Media Processing, and Signed-URL Delivery | Proposed | 2026-05-23 | Core | Stand-up decision for `HoneyDrunk.Files` as the Core sector's owner of **bytes + bytes-metadata** for every Grid consumer — the blob-storage analog of `HoneyDrunk.Data` (structured persistence) and `HoneyDrunk.Vault` (secrets). Five frozen contracts: `IFileStore`, `IFileUploadSession`, `IFileMetadata`, `IFileProcessor`, `IFileQuotaPolicy`. **Azure Blob Storage** as default backing; **signed-URL direct-to-blob upload pattern** (not byte-proxying through the Node); per-tenant container isolation; Azure Defender for Storage for malware scanning on upload; CDN-fronted public delivery vs short-lived SAS for private. Driven by PDR-0005 Hearth (journaling-town photo uploads), PDR-0003 Lately, PDR-0006 Currents, PDR-0008 Curiosities all assuming user-uploaded media plus ADR-0049 classifying journal/photo/voice as **Restricted** tier requiring explicit storage discipline. Hard-delete cascade integrates with ADR-0050 tenant-offboarding and GDPR Art. 17 erasure. Notify treats attachments as opaque payload (delivery, not storage); Files is the Grid-side home. Scaffold ships Abstractions + InMemory adapter only — **no Azure backing on day one**; that lands when first PDR consumer activates Files. |
| [ADR-0062](ADR-0062-inbound-webhook-verification.md) | Inbound Webhook Verification and Receiver Pattern | Proposed | 2026-05-23 | Core / cross-cutting | Settles the Grid-wide inbound-webhook receiver pattern before four independent surfaces land in one release window (`HoneyDrunk.Billing.Webhooks` Stripe per ADR-0037, Notify's Resend/Twilio status webhooks per ADR-0027 / ADR-0038, Observe's GitHub connector per ADR-0010, Communications Operator-approval subscribers per ADR-0019). **HMAC algorithm**: per-provider-as-emitted (Stripe's `t=...,v1=hmac_sha256`, GitHub's `sha256=...`, Svix, Twilio's HMAC-SHA1) — receivers verify what the provider sent, no Grid-uniform header rewriting. **Replay window: 5 minutes, hard-pinned** Grid-wide (matches Stripe default + OWASP guidance) — receivers without a signed timestamp fall back to nonce-based replay protection via the dedup store. **Raw-body preservation** via Kernel-owned middleware (signature verification requires the raw bytes, not the deserialized model). Standardized response codes (`400` on signature/replay failure, `2xx` only after persistence). Signing-secret rotation composes with ADR-0006; payload-side PII handling composes with ADR-0049. Outbound webhooks (Studios-as-emitter) explicitly deferred to a future ADR. |
| [ADR-0063](ADR-0063-date-time-and-clock-policy.md) | Grid-Wide Date, Time, and Clock Policy | Proposed | 2026-05-23 | Core / cross-cutting | Commits `System.TimeProvider` as the **Grid-wide clock abstraction** — every Node reads "now" via `TimeProvider.GetUtcNow()`, direct calls to `DateTimeOffset.UtcNow`/`DateTime.UtcNow`/`.Now` are **forbidden** in production code (enforced by analyzer rule in `HoneyDrunk.Standards`). `TimeProvider` is a BCL type — Kernel does **not** wrap it with an `IGridClock` shim (ceremony without value). DI helpers `AddSystemTimeProvider()` (production wires `TimeProvider.System`) and `AddFakeTimeProvider()` (tests wire `Microsoft.Extensions.TimeProvider.Testing.FakeTimeProvider`) ship in `HoneyDrunk.Kernel`. **UTC at rest, everywhere** — all persisted timestamps, Service Bus message timestamps, audit records, cache TTL boundaries, idempotency expiry, configuration timestamps are UTC; timezone conversion happens at the presentation boundary only. ISO 8601 with `Z` for instants, `YYYY-MM-DD` for dates, ISO 8601 duration strings for spans on the wire (pins the serialization detail [ADR-0057](./ADR-0057-public-http-api-versioning-and-client-sdk-strategy.md) deferred). Cadence specification format (cron strings, 5-field UTC) consumed by [ADR-0068](./ADR-0068-background-job-and-recurring-work-substrate.md). Documented opt-out attribute for third-party SDK interop and pre-DI host bootstrap. |
| [ADR-0064](ADR-0064-prompt-and-persona-registry-with-versioning.md) | Prompt and Persona Registry with Versioning | Proposed | 2026-05-23 | AI / cross-cutting | Fills the missing-parallel-to-ADR-0041 gap: a **prompt-and-persona registry** that does for prompts what `IModelRegistry` does for models. Storage is a **dedicated public repo `HoneyDrunk.Prompts`** with text files + YAML frontmatter, distributed as a NuGet content-and-runtime hybrid package — every consumer (`HoneyDrunk.AI`, `Agents`, `Operator`, `Memory`, `Knowledge`, `Evals`, `Lore`, Honeyclaw build) pins the version they build against. **Persona vs Prompt distinction** load-bearing: Persona = stable named identity (Honeyclaw bear-keeper, Evals judge, Operator safety filter) with `system_prompt` body and long lifecycle; Prompt = task-specific template with parameterized `body`, `parameters` declaration, optional `extends` reference to a persona. Versioning: **semver per package, content-hash per file emitted at use time** (so every Audit emit per ADR-0030 carries the exact text fingerprint; Evals per ADR-0023 can pin regressions to a prompt change). Rejected alternatives: App Configuration snapshots (multi-KB prompts fit poorly, no code-review diff surface), database table with content-addressed hash (loses reviewable text diffs). Paired stand-up ADR for the Prompts Node follows immediately. |
| [ADR-0065](ADR-0065-multi-service-local-dev-orchestration-aspire.md) | Multi-Service Local Dev Orchestration and .NET Aspire Stance | Proposed | 2026-05-23 | Ops / cross-cutting | Adopts **.NET Aspire** as the Grid's committed local-dev orchestrator, **local-dev only** — Aspire is not a production deployment authority. AppHost shape is two-tier: **per-Node AppHost** alongside each containerized Node's solution models that Node's processes plus dependencies (`{Node}.AppHost`); **per-scenario AppHost** in a dedicated `HoneyDrunk.Workshop` repo for cross-Node integration scenarios (`Workshop.Notify-Plus-Communications.AppHost`, `Workshop.Agents-Plus-Memory-Plus-AI.AppHost`, `Workshop.Full-Local-Grid.AppHost`). Aspire's OTLP dashboard composes with Pulse for the local trace/log/metric story. Triggered by Notify (Functions + Worker) + Pulse.Collector being the first multi-process dev deploy. Rejected: per-Node `launchSettings.json` only (collapses past three processes), docker-compose (not .NET-native, no built-in dashboard), ad-hoc PowerShell scripts (collapses past three scenarios), Tye/k3d/Tilt/Skaffold (none .NET-native, Tye effectively deprecated). Aspire is young (GA 2024); honest acknowledgment that the API will evolve, with the mitigation that AppHost projects are thin per-Node migrations rather than Grid cascade. |
| [ADR-0066](ADR-0066-health-readiness-liveness-endpoints.md) | Health, Readiness, and Liveness Endpoint Contract | Proposed | 2026-05-23 | Core / cross-cutting | Settles the Grid-wide three-endpoint health contract on top of existing `IHealthContributor` / `IHealthCheck` primitives in `HoneyDrunk.Kernel.Abstractions`. Every HTTP-fronted deployable Node exposes **`/health/live`** (liveness — process alive, no dependency checks; consumed by Container Apps `livenessProbe` per ADR-0015), **`/health/ready`** (readiness — required dependencies reachable, Node at `Ready` lifecycle stage; consumed by `readinessProbe`), and **`/health`** (full aggregate of every registered `IHealthContributor` — auth-required, not consumed by probes). Resolves the existing drift: Pulse.Collector's static `200 OK` placeholders, Notify's `INotifyHealthContributor` parallel-not-unified-with-Kernel-`IHealthContributor` shape. Three-endpoint shape rejected alternatives include two-endpoint (conflates restart-vs-no-restart semantics), one-endpoint with query param (orchestrator-hostile), Kubernetes-style `/healthz` (Grid is not on Kubernetes). Container Apps probe defaults pinned; PII / connection-string exposure in body forbidden; auth posture (anonymous `/health/live` + `/health/ready` for probes, auth-required `/health` aggregate). Telemetry contribution to Pulse via probe-outcome metric. |
| [ADR-0067](ADR-0067-inbound-rate-limiting-and-quota-enforcement.md) | Inbound Rate Limiting and Quota Enforcement | Proposed | 2026-05-23 | Core / cross-cutting | Picks the substrate ADR-0026 D4's `ITenantRateLimitPolicy` deferred: **ASP.NET Core `RateLimiter` middleware** (`Microsoft.AspNetCore.RateLimiting`) as the primary in-process substrate (first-party, in-process, free; `PartitionedRateLimiter` keys natively on `TenantId` per ADR-0026), **complemented by Cloudflare at the edge** for L7 DDoS shielding. Notify Cloud per-tier limits pinned (Free / Pro / Scale per ADR-0037) — derived from `NotifyCloudTenantTier` per ADR-0027 D6. Standardized 429 response envelope (RFC 7807 Problem Details, `Retry-After` header, `RateLimit-*` success-side headers so clients can self-throttle). **Rate limit vs quota distinction** load-bearing: per-second/minute limits via in-process limiter; per-month quotas via separate counter store (Azure Storage Tables or Cache Node-backed once a distributed backing lands per ADR-0058/0059). Distributed coordinated limiter deferred until multi-replica scale-out triggers it. Rejections emit Audit per ADR-0030 (`RateLimitRejected`) and Pulse metric with tags `tenant_id`, `endpoint`, `tier`, `outcome` per ADR-0010. |
| [ADR-0068](ADR-0068-background-job-and-recurring-work-substrate.md) | Background Job and Recurring Work Substrate | Proposed | 2026-05-23 | Core / Ops · cross-cutting | Defers `HoneyDrunk.Jobs` as a Node and pins the substrate per workload category. **Three distinct categories**: (1) **CI/CD and ops cron** (nightly-deps, nightly-security, hive-sync, grid-health aggregator) stays on **GitHub Actions cron** per ADR-0012 — out of scope here; (2) **In-Node background processing** (Notify retry pump, AI cost-ledger drain, audit batch flush) uses **`IHostedService` / `BackgroundService`** inside the Node's host process; (3) **Cross-Node recurring orchestration** (Communications drip campaigns, Vault rotation, tenant-lifecycle scheduled tasks, billing reconciliation) uses **Azure Container Apps Jobs** (the Jobs-shaped sibling of Container Apps per ADR-0015 — scheduled and event-triggered, single-invocation rather than long-running). Vault.Rotation's existing Functions-timer-trigger pattern **grandfathers** (D7); new cross-Node recurring work goes to Container Apps Jobs. Mandatory idempotency on every job per ADR-0042; retry policy defaults; observability via Pulse per ADR-0010. Cron format (5-field, UTC) inherits from [ADR-0063](./ADR-0063-date-time-and-clock-policy.md) D6. New `job-deploy-container-apps-job.yml` reusable workflow lands in HoneyDrunk.Actions. |
| [ADR-0069](ADR-0069-currency-handling-and-money-representation.md) | Currency Handling and Money Representation | Proposed | 2026-05-23 | Core (Kernel.Abstractions) · Ops (first consumers: Billing, Notify Cloud) · cross-cutting | Commits **`Money` as a record** in `HoneyDrunk.Kernel.Abstractions.Money` carrying `decimal Amount` and a `CurrencyCode` (ISO 4217 alpha-3 validated; per-Grid naming rule it is `Money` not `IMoney`). Arithmetic operators **throw on currency mismatch** — no silent FX coercion. Banker's rounding (`MidpointRounding.ToEven`) as the canonical mode. JSON serialization shape: `amount` as **string** (preserves `decimal` precision through JS clients), `currency` as ISO 4217 alpha-3 — cross-references ADR-0057's serialization envelope. **Multi-currency stance**: the Grid stores money in the original transacted currency; FX conversion happens at Stripe's boundary per ADR-0037 or at the presentation boundary, never silently in transit. **Cost-ledger denomination** pinned to USD per ADR-0052 (today's AI providers all bill USD; FX conversion deferred until first non-USD provider lands). Audit events use the **two-field convention** `monetary_value` + `currency_code` per ADR-0030 (`BillingEvent` itself does **not** carry `Money` — its `Quantity` + `UnitOfMeasure` shape stays per ADR-0026 D5). Driven by Notify Cloud GA being the first commercial product emitting money-changing events. |
| [ADR-0070](ADR-0070-frontend-platform-stack.md) | Frontend Platform Stack — React for Web, Blazor for Simple Admin, React Native + Expo for Mobile | Proposed | 2026-05-23 | Creator / cross-cutting (frontend choice affects every consumer-facing PDR) | Three-stack split for the Grid's frontend surfaces, settling the previously-undecided mobile platform question. **React (+ TypeScript)** is the default for **consumer-facing web** — Hearth, Lately, Currents, Curiosities, every PDR-driven consumer surface; Vite as default build tooling, Next.js when server-rendering needs justify it. **Blazor** is permitted for **admin surfaces only when simple enough** (decision test: low-interactivity CRUD/forms; no rich client-side state, no animation-heavy UX) — productivity win of staying in .NET (one language end-to-end, shared types, no TS translation layer) outweighs the lost React ecosystem for Notify Cloud admin surfaces. **React Native + Expo** for **mobile** — Expo Notifications aligns with [ADR-0073](./ADR-0073-notify-default-providers.md)'s push provider choice. AI-multiplier reasoning: Claude/Codex/Copilot's deepest 2026 training corpus is React/TypeScript, second-deepest is .NET — stacks outside that gradient (Flutter, Vue, Svelte, Solid, Angular, native Swift/Kotlin) deliver less per token of agent attention. Paired with [ADR-0071](./ADR-0071-stand-up-honeydrunk-web-ui-node.md) which stands up the design-system Node that bridges all three stacks. |
| [ADR-0071](ADR-0071-stand-up-honeydrunk-web-ui-node.md) | Stand Up the HoneyDrunk.Web.UI Node — Shared Frontend Design System | Proposed | 2026-05-23 | Creator (anchor) | Stand-up decision for `HoneyDrunk.Web.UI` as the **anchor of the previously-empty Creator sector** — owns the **design substrate** consumed by every other frontend surface. Owns: design tokens (color, spacing, typography, radii, shadows, motion, breakpoints — shipped as CSS variables and as a stack-agnostic JSON tokens file), primitive CSS (reset, base typography, utility classes), per-stack component contracts. Per-stack package layout from D6: `@honeydrunk/web-ui-tokens` + `@honeydrunk/web-ui-css` (stack-agnostic), `@honeydrunk/web-ui-react` (React), optional `HoneyDrunk.Web.UI.Blazor`, optional `@honeydrunk/web-ui-native` (React Native). Paired with [ADR-0070](./ADR-0070-frontend-platform-stack.md) — Web.UI is the cross-stack reconciliation point for the three frontend stacks. Studios is the established React surface today and migrates to consume Web.UI tokens at the first natural opportunity (Web.UI is consumed by Studios, **not folded into** Studios). Scaffold packet ships tokens + CSS at minimum on day one; per-stack React/Blazor/RN packages follow as consumers activate them. |
| [ADR-0072](ADR-0072-data-access-stance-ef-core-default-dapper-hot-path.md) | Data Access Stance — EF Core Default, Dapper for Hot-Path Reads | Proposed | 2026-05-23 | Core / cross-cutting | Pins **Entity Framework Core (current LTS)** as the Grid's default ORM for every Node touching a relational store — ratifies the implicit ORM behind ADR-0048's EF Migrations choice and the model-attribute mechanism behind ADR-0049's PII classification. `Microsoft.EntityFrameworkCore.SqlServer` for Azure SQL backings, `Microsoft.EntityFrameworkCore.Npgsql` for Postgres backings. **Dapper is the scoped exception** for hot-path reads where EF generates measurably worse SQL or where allocation profile matters — measurement is a query-plan-explored, profiler-confirmed determination, not a vibe; the packet introducing a Dapper query includes the EF-generated query, the hand-written replacement, and benchmark evidence in the PR. DbContext-per-tenant or schema-per-tenant patterns map cleanly onto ADR-0050's per-tenant partition mechanic; Audit's append-only write path (per ADR-0030/0031) is a candidate Dapper exception but EF stays the default. Rejected: Marten, RepoDb, Pomelo, raw ADO.NET as default, EF 6 / Classic for new work. |
| [ADR-0073](ADR-0073-notify-default-providers.md) | Notify Default Providers — Resend (Email), Twilio (SMS), Expo (Push) | Proposed | 2026-05-23 | Ops | Fills [ADR-0019](./ADR-0019-stand-up-honeydrunk-communications-node.md)'s provider-choice gap with canonical defaults for Notify's `IEmailSender` / `ISmsSender` / future `IPushSender` slots. **Resend** as default email — best-in-class API ergonomics, react-email first-class integration, predictable pricing curve through Notify Cloud's tenant-ceiling volumes, DKIM/SPF/DMARC tooling pairs with ADR-0038. **Twilio** as default SMS (explicitly **tentative**) — re-evaluate at first cost-pressure inflection. **Expo Notifications** as default push — aligns with [ADR-0070](./ADR-0070-frontend-platform-stack.md) D3's React Native + Expo mobile-platform choice, end-to-end coherent. Provider abstraction held: defaults are **not exclusive bindings**; tenants and consumer PDRs may choose alternatives via the provider-slot pattern. API keys in Vault per ADR-0005, rotated per ADR-0006 Tier 2; webhook callbacks (bounce, complaint, delivered, opened, clicked) verified per ADR-0062. Rejected: SendGrid, AWS SES, Postmark, Mailgun as defaults. |
| [ADR-0074](ADR-0074-testing-library-stack.md) | Testing Library Stack — xUnit + NSubstitute + AwesomeAssertions | Proposed | 2026-05-23 | Meta / cross-cutting | **Specification ADR amending [ADR-0047](./ADR-0047-testing-patterns-and-tooling.md) D2** — promotes the library-pick rationale from D2 prose into a standalone, frontmatter-citable decision so future Node-scaffold packets (Identity per ADR-0060, Files per ADR-0061, Cache per ADR-0059, Web.UI per ADR-0071) can cite a canonical anchor in their `Directory.Build.props`. **xUnit v2.x** (xUnit v3 when stabilized) as the test framework — already in use, best parallel-test story among .NET-native frameworks, fixture model (`IClassFixture<T>` / `ICollectionFixture<T>`) fits the Grid's contract-shape canary patterns. **NSubstitute** as the mocking library — Moq is **not used for new work** due to the 2023 SponsorLink stewardship incident. **AwesomeAssertions** (MIT fork of FluentAssertions v7) — FluentAssertions v8 went commercially relicensed October 2024 and is not adopted. **coverlet** for coverage. Stewardship-and-licensing reasoning made load-bearing rather than buried in ADR-0047 prose. Reviewer agent's testing-quality category per ADR-0044 D3 checks for this committed stack at PR time. |
| [ADR-0075](ADR-0075-documentation-tooling.md) | Documentation Tooling — Scalar (In-Product OpenAPI) and Docusaurus (Public Docs) | Proposed | 2026-05-23 | Meta / cross-cutting | Two-tool split for the Grid's two distinct documentation surfaces. **Scalar** (`Scalar.AspNetCore`) replaces Swagger UI as the **in-product OpenAPI renderer** in every Node that exposes an OpenAPI spec per [ADR-0057](./ADR-0057-public-http-api-versioning-and-client-sdk-strategy.md) — modern UX, better large-spec performance (Notify Cloud's OpenAPI will be large), MIT-licensed with healthy stewardship (matches ADR-0074's license-hygiene principle), theming hooks align with ADR-0071's Web.UI design tokens. Per-environment availability: dev/staging always enabled; prod enabled at Node's discretion (Notify Cloud's public API keeps Scalar in prod; internal-only Identity/Audit may keep dev/staging-only). **Docusaurus** as the canonical static-site generator for **standalone public documentation sites** — used **when a Node warrants its own doc site**, not by default; React-based (aligns with [ADR-0070](./ADR-0070-frontend-platform-stack.md) D1). Existing Swagger UI usage grandfathers, migrates opportunistically. Studios website is **not in scope** (separate Node and product per ADR-0071 D3). Rejected: GitBook, MkDocs Material, Astro Starlight, DocFX, plain GitHub Pages. |
| [ADR-0076](ADR-0076-cache-backing-azure-cache-for-redis.md) | Cache Backing — Azure Cache for Redis with Cost-Aware Sizing | Proposed | 2026-05-23 | Core / cross-cutting | Fills [ADR-0058](./ADR-0058-grid-wide-caching-strategy.md) D8's deferred first-distributed-backing decision. **Azure Cache for Redis** as the canonical `ICacheStore<T>` distributed backing, shipped as `HoneyDrunk.Cache.Redis` built on `StackExchange.Redis`. Per-environment instance `redis-hd-{env}` (Basic C0 ~$16/mo dev, Standard C1 ~$80/mo staging, Standard tier baseline for prod with sizing per Node load — no Premium-tier-required workload today). Managed-identity authentication where supported (Standard tier+); access keys in Vault per ADR-0005 as fallback. Cache-invalidation Lane 2 (Service Bus per ADR-0028) composes naturally — no bespoke pub-sub-on-Redis pattern. **Standard Redis protocol only** — no Azure Redis Modules (RedisJSON, RedisSearch, RedisTimeSeries); protocol portability is the cheap vendor-exit hedge formalized in [ADR-0080](./ADR-0080-vendor-lock-in-posture-and-exit-readiness-hedges.md). Rejected: self-hosted Redis on Container Apps, Azure Cosmos with TTL, KeyDB / DragonflyDB / Garnet, managed Redis from other vendors (Upstash, Redis Cloud) as default. |
| [ADR-0077](ADR-0077-infrastructure-as-code-bicep.md) | Infrastructure-as-Code — Bicep (Azure-native) | Proposed | 2026-05-23 | Ops / cross-cutting | Commits **Bicep** as the IaC tool for every Azure resource the Grid provisions — Container Apps, Key Vault, App Configuration, Service Bus namespaces, Event Grid topics, Storage Accounts, Application Insights, Azure Cache for Redis, anything else. Resolves the current mix of manual Portal clicks, scattered Azure CLI scripts, and no version-controlled IaC (the bus-factor failure mode the charter warns against). Modularize by concern (per-Node `infra/` folder with `.bicep` files); `HoneyDrunk.Actions` ships `job-deploy-bicep.yml` reusable workflow. **No raw ARM JSON** for new work, **no Azure CLI scripts as primary IaC** (CLI permitted for experimental work only), **no manual Portal provisioning** for new resources (existing resources grandfather). Honest acknowledgment of the **Azure-deep posture** this commitment reinforces — Bicep is Azure-only, no Terraform's `azurerm`/`azapi` provider-lag, no state-file overhead, but no multi-cloud option either. Pre-stages the modularization that makes ADR-0080's vendor-exit playbook cheaper. Rejected: Terraform, Pulumi, raw ARM JSON, Azure CLI scripts as primary IaC. |
| [ADR-0078](ADR-0078-end-user-identity-entra-external-id.md) | End-User Identity — Microsoft Entra External ID | Proposed | 2026-05-23 | Core | Fills [ADR-0060](./ADR-0060-stand-up-honeydrunk-identity-node.md) D2's deferred vendor decision before Hearth (PDR-0005) pulls on Identity. **Microsoft Entra External ID** as the identity provider for all end-user authentication — every consumer-app user (Hearth, Lately, Currents, Curiosities, future consumer PDRs) and every Notify Cloud tenant-operator user. Single Entra External ID tenant for the Grid; per-application App Registrations (one per consumer app + one for Notify Cloud tenant operators). OAuth 2.1 with PKCE per ADR-0057; OpenID Connect as identity protocol. **Explicitly not Azure AD B2C** — B2C is being sunset for new tenants; Entra External ID is Microsoft's active CIAM successor. Free up to 50K MAU covers every reasonable v1 consumer-app trajectory. **OIDC-standard claims only** — no Entra-specific claim shapes leak into the Identity Node's contract surface; the cheap vendor-exit hedge formalized in [ADR-0080](./ADR-0080-vendor-lock-in-posture-and-exit-readiness-hedges.md). Custom-domain support (`auth.honeydrunkstudios.com`); passkey/FIDO2/WebAuthn built in. Rejected: Auth0, Clerk, Supabase Auth, self-hosted credential storage at v1. |
| [ADR-0079](ADR-0079-multi-perspective-pr-review-stack.md) | Multi-Perspective PR Review Stack — Copilot + CodeRabbit + Grid-Aware Agent (Codex + Claude) | Proposed | 2026-05-23 | Meta | Settles the canonical PR-review stack and **satisfies [Invariant 53](../constitution/invariants.md)** (two independent LLM-review perspectives on high-risk Nodes) through dual-model execution of the Grid-aware agent. **Three reviewers on every non-draft PR by default**: (1) GitHub Copilot Code Review (GitHub-native, zero marginal cost, not Grid-aware); (2) **CodeRabbit** (~$24/dev/mo, third-party-AI independence from Microsoft and Anthropic, `.coderabbit.yaml` rule system, not Grid-aware) — **reverses ADR-0011 D11's CodeRabbit rejection**; (3) **Grid-aware `review` agent via Codex** (OpenClaw-triggered, Grid-aware via ADR-0044). **A fourth reviewer runs on substantive PRs**: Grid-aware `review` agent via **Claude** (Anthropic's native Claude Code on the web GitHub integration, billed against the operator's Claude Max subscription via the June 15 2026 Claude Agent SDK credit pool — economic shape unavailable when ADR-0044 was authored). Different model family, same `.claude/agents/review.md` definition, same rubric. **Substantive-PR classifier** pinned (excludes typo/docs-only/README PRs — paying for review where review value is near zero is exactly the "performing visibility" failure mode the charter forbids). Per-PR cost ceiling bounded; reviewer count capped at four. Superseded in part by ADR-0086 (Reviewer 3 transport, Reviewer 4 substrate). |
| [ADR-0080](ADR-0080-vendor-lock-in-posture-and-exit-readiness-hedges.md) | Vendor Lock-In Posture and Exit-Readiness Hedges | Accepted | 2026-05-23 | Meta / Infrastructure / cross-cutting | Generalizes the same hedge pattern cited by ADR-0076 D3 (Redis-protocol-only), ADR-0077 D5 (Bicep modules by concern), and ADR-0078 D3 (OIDC-standard claims only) into the Grid's **chosen vendor-posture discipline**. Every vendor relationship is assigned **one of three postures**: **Accept** (deep, intentional — productivity gain outweighs portability concern, multi-month exit understood and accepted; e.g. Azure, GitHub), **Hedge** (active — small ongoing discipline keeps options open, exit bounded to weeks; e.g. Cloudflare, Stripe, Twilio), **Abstract** (already portable — Grid-defined interface with multiple compatible implementations, switching in days; e.g. Anthropic, OpenAI, Resend). Per-vendor posture table commits the assignment and the specific hedges in place across every load-bearing vendor. **"Multi-cloud," "vendor-neutral," "cloud-agnostic"** explicitly **not** in the Grid's vocabulary — multi-cloud-by-default is a permanent tax the Grid does not pay. Names re-evaluation triggers (pricing/terms/acquisition/deprecation/alignment-drift) and the honest exit cost per vendor; does not budget portability work, does not set kill clocks. Resolves the dangling "future vendor-exit playbook" pointers in ADR-0076/0077/0078. |
| [ADR-0081](ADR-0081-home-server-for-openclaw-and-local-agent-infrastructure.md) | Home Server for OpenClaw and Local Agent Infrastructure | Proposed | 2026-05-24 | Meta / Ops / AI | Introduces a small always-on **home server** as the preferred host for OpenClaw, webhook bridges, scheduled local automations, and local-agent experimentation — explicitly **not** a production application host for public HoneyDrunk customer-facing services (those remain on cloud hosts per ADR-0015 / ADR-0077). The home server is an **operator-owned local control-plane host** for development automation, review automation per ADR-0044, and agent infrastructure. Owns workloads that must survive workstation sleep/reboots: OpenClaw Gateway and Honeyclaw runtime state, the ADR-0044 webhook bridge and Cloudflare Tunnel process, scheduled local automation jobs, local agent sandboxes / worktrees / experimental runtimes, lightweight observability for local automation. **No router/firewall exposure** — public ingress via Cloudflare Tunnel only, with one tiny local bridge per public integration and application-layer signature/secret verification before invoking work; OpenClaw dashboard/gateway never tunneled wholesale. Hardware target favors **boring reliability over homelab complexity** (small NUC/mini-PC class, low idle power, headless Linux). Workstation remains the primary interactive development machine. |
| [ADR-0082](ADR-0082-canonical-node-standup-procedure.md) | Canonical Node Standup Procedure | Accepted | 2026-05-25 | Meta | Names **the canonical procedure for standing up a new Grid Node**, closing the drift pattern in which every standup (Audit, Notify Cloud, the AI sector, Cache, Identity, Files) re-derived its own checklist from the most recent precedent. **One canonical document** at `constitution/node-standup.md` (new) is the single source of truth for every standup packet, ADR, and AI agent. **Six Node classes** committed (`core-dotnet-abstractions-runtime`, `core-dotnet-runtime-only`, `ops-deployable-dotnet`, `meta-docs`, `ai-seed`, `studios-typescript`), declared via new `node_class` ADR frontmatter; new classes need a one-row amendment, not a new ADR. **Three-phase procedure** with hard prerequisite gates: Phase A Architecture registration (catalogs + context folder + sector row, lands as a single Architecture packet) → Phase B human-only GitHub repo creation (visibility, branch protection, label seeding, OIDC subject pattern, org-secret repo binding per D8) → Phase C scaffold landing (solution layout, packages, contract surface, CI workflows, contract-shape canary). **New invariant 102** commits the ten-artifact requirement before any non-bootstrap PR merges: catalog rows in `nodes.json` + `relationships.json` + `grid-health.json`, the five-file `repos/{Node}/` context folder, `constitution/sectors.md` row, `repo-to-node.yml` mapping in `HoneyDrunk.Actions`, `.honeydrunk-review.yaml` with explicit `enabled` flag, `pr.yml` calling `pr-core.yml`, branch protection requiring `pr-core / core`, **plus org-secret repo binding** for every org Actions secret the repo's workflows reference (minimum `SONAR_TOKEN` per ADR-0011 D11; conditional `NUGET_API_KEY`, the ADR-0014 hive-sync stack, the ADR-0084 Discord webhook stack, `OPENCLAW_GRID_REVIEW_WEBHOOK_SECRET`) — enforced procedurally via the `review` and `node-audit` agents and hive-sync drift detection (item 10 is outside the `review` agent's admin scope at v1; operator-memory plus the per-class binding matrix in `constitution/node-standup.md` carry it), no CI gate committed by this ADR. **Follow-up work** lands as `constitution/node-standup.md` plus five per-class walkthroughs in `infrastructure/walkthroughs/node-standup-{core-dotnet,ops-deployable-dotnet,meta-docs,ai-seed,studios-typescript}.md` plus `infrastructure/walkthroughs/org-secret-repo-binding.md` per D8 (the seventh walkthrough mandated by D7 — the per-secret access-policy click-path the per-class walkthroughs reference). Worked example: ADR-0031 Audit standup (packets 02 and 03) is the gold-standard reference. |
| [ADR-0083](ADR-0083-external-saas-credential-rotation.md) | Sensitive Inventory and External-SaaS Credential Rotation Procedure | Accepted | 2026-05-25 | Infrastructure / cross-cutting | Closes the gap left by ADR-0005 / ADR-0006: the class of credentials and load-bearing identifiers the Grid depends on but that **do not live in any `kv-hd-*` vault** — `SONAR_TOKEN` (60-day SonarCloud free-tier cap), `NUGET_API_KEY`, GitHub PATs (`GH_ISSUE_TOKEN`, `HIVE_FIELD_MIRROR_TOKEN`, `LABELS_FANOUT_PAT`), the ADR-0044 webhook signing secret, the OpenClaw GitHub App private key, plus the imminent commercial trio (Stripe / Resend / Twilio) and non-rotating identifiers (`HIVE_APP_ID`, `AZURE_TENANT_ID`, OIDC subject patterns, resource identifiers). **Broadened scope** per operator framing: the inventory is a **registry of what the Grid holds** (not a secret store), of which rotation-needing credentials are one populated subset — closes the lottery-bus-factor failure mode. Commits **`infrastructure/reference/sensitive-inventory.md`** as the canonical Markdown index with eleven required columns including `Kind`, `Use Cases`, and `Rotates`; per-provider walkthroughs under `infrastructure/walkthroughs/` for **rotation-needing** entries only; standing GitHub issues labeled `external-credential-rotation` as the tracking surface (not calendar reminders); a new scheduled `external-credentials-check.yml` workflow in HoneyDrunk.Actions performing T-30 / T-7 / T+0 drift detection filtered by `Rotates: yes`. **`HoneyDrunk.Vault.Rotation` does not expand** to cover external-SaaS rotation — manual rotation with disciplined inventory and tracking at fewer-than-ten active credentials is cheaper than a per-provider rotation Node. ADR-0082 D5 standup procedure gains a sensitive-inventory onboarding step per D6. New invariant binds inventory-membership for everything load-bearing plus the rotation-discipline triplet (walkthrough + standing issue + escalation) for `Rotates: yes` rows; complements (does not replace) Invariant 20. |
| [ADR-0084](ADR-0084-discord-operator-alerts-surface.md) | Discord as the Canonical Operator-Alerts Surface | Proposed | 2026-05-25 | Ops / Meta / cross-cutting | Pins **Discord as the canonical operator-alerts surface** for the Grid — the real-time, day-to-day operational pager surface for the solo operator across CI failures, security signals, agent activity, release events, hive-sync drift, and ADR-0083 credential-rotation escalation. **Explicitly not** a `HoneyDrunk.Communications` / `HoneyDrunk.Notify` / Notify Cloud / Notify Cloud community concern — operator alerts are pre-customer, internal, pre-SLA, pre-tenant; reusing customer-notification substrate would muddle the ADR-0019 boundary. Seven dedicated channels (`#ops-alerts`, `#security-alerts`, `#agent-activity`, `#hive-activity`, `#release`, `#announcements`, `#audit-sensitive`); **native Discord webhooks, one per channel, no bot bridge** — matches the ADR-0080 Hedge posture (webhook-URL change per emitter swaps to Slack / Mattermost / Matrix / Teams in days). Seven `DISCORD_WEBHOOK_*` GitHub org secrets (inventoried per ADR-0083 D2, `Rotates: no` — non-expiring, rotate-on-compromise). Defined alert-source roster mapped to a D6 routing table living at `constitution/alert-routing.md`; new reusable workflow `job-discord-notify.yml` in HoneyDrunk.Actions as the single CI-side seam (plus a home-server helper script for ADR-0081 emitters). Payload rules: **no secret values, no customer PII, no full stack traces** — Invariant 8 extended to webhook payloads. Phased rollout (CI failures + release events first, review pipeline + hive-sync second, security signals + budget alerts third). New invariant binds every operator-actionable Grid event through the reusable workflow / helper-script seam. ADR-0082 D5 standup procedure and ADR-0080 D2 vendor-posture table both amended by follow-up (Discord row + standup onboarding step). |
| [ADR-0085](ADR-0085-grid-wide-documentation-currency-agent.md) | Grid-Wide Documentation Currency Agent (`docs-sync`) | Proposed | 2026-05-26 | Meta | Stands up **`docs-sync`** as a new Meta-sector scheduled agent that detects and reconciles **cross-repo documentation drift Grid-wide** (root + per-package READMEs, root + per-package CHANGELOGs, `AGENTS.md` / `CLAUDE.md` / `.github/copilot-instructions.md`, `docs/` content) — closes the substrate-shaped concern (Grid-wide, no current owner, cheap to drift, expensive to find later) left uncovered by `hive-sync` (Architecture-repo-only per ADR-0014 D4), `node-audit` (on-demand, one-Node-at-a-time), and `review` (per-PR, diff-bound only). Same hosting model as `hive-sync` / `site-sync`. **Scope:** existence + accuracy across two concerns — accuracy v1 is **resolvable by symbol lookup or string comparison** (no compilation, no shell, no HTTP); explicitly defers code-example execution, Vale-class prose quality, OpenAPI rendering (ADR-0075), Docusaurus sites, and XML doc comments (Invariant 13 / `HoneyDrunk.Standards`). Six finding categories (missing required artifacts, version drift, symbol-reference drift, catalog-reference drift, dependency-graph drift, agent-instruction drift). **Authority model:** one PR per affected repo per run against the target's default branch for mechanical fixes (version drift, rename-aware link rewrites, dead intra-repo links, conditional skeleton README generation); editorial-judgment findings (symbol drift, dependency claims, agent-instruction drift) surface in PR body as TODO blocks or fall back to `generated/issue-packets/proposed/` packets honoring ADR-0043 D3. PR metadata: `Authorship: agent-claude-code` + `Out-of-band reason:` pointing at the per-run report. **Weekly full sweep (Friday cadence)** via OpenClaw scheduled trigger; per-run report at `generated/docs-sync-reports/{YYYY-MM-DD}.md`. **GitHub App installation token** (`docs-sync` app) replaces classic PAT for cross-repo PR authority; private key in `kv-hd-docs-sync-prod` per ADR-0005, rotated per ADR-0006 Tier 2. Six-phase rollout (report-only → version-drift PRs → catalog-rewrite PRs → drift detection broadened → skeleton-README generation → cadence + invariant decisions). Three candidate invariants deferred to Phase 6. |
| [ADR-0086](ADR-0086-pull-based-local-worker-grid-review-runner.md) | Pull-Based Local Worker as the Grid Review Runner | Accepted | 2026-05-26 | Meta | Replaces ADR-0044 D1/D5's signed-webhook -> OpenClaw review transport and ADR-0079 D1 Reviewer 3 / D2 Reviewer 4 substrates with a pull-based local worker that queues via GitHub labels/comments, then runs Codex CLI and Claude Code CLI under subscription auth while preserving the existing Grid-aware review discipline. |
| [ADR-0087](ADR-0087-per-change-risk-scoring-for-double-review.md) | Per-Change Risk Scoring for the Double-Review Gate | Proposed | 2026-05-30 | Meta | Replaces the **static per-Node "high risk" notion** inherited by ADR-0086 D8 from ADR-0044 D8 (a `review_risk_class` per-repo flag that was a phantom — nothing populated it and the worker never read it) with a **per-change deterministic weighted-signal scorer** that decides, per PR, whether the dual Codex + Claude Code double-review fires. **Four signals in binding order `sensitivity ≥ blast-radius > boundary-spread > size`:** size alone can never trip the gate; any single `forced` sensitive-path match (Vault secret-resolution, credentials, `ISecretStore`-class contracts) trips it alone **regardless of authorship** — human-authored PRs touching the forced subset are gated too. Signals derive from a new **`catalogs/review-risk-signals.json`** (sensitivity path patterns + a `repo_to_node_id` join into `relationships.json` for blast-radius), never duplicating the sensitive-inventory or relationship graph. Scorer runs as a step in `job-review-request.yml`, fetches catalogs **cross-repo via `gh api` raw-fetch** (the workflow checks out only the PR repo) and **fails open to shadow** on fetch failure; ships in **shadow posture** with an observable `gate_mode:` marker, flipped to enforce per-repo via `.honeydrunk-review.yaml`. **Boundary spread** is a path-only structural signal (distinct package/path-root boundaries + code-vs-docs classification), explicitly not a line-count proxy — deeper structural complexity is documented as not computable at the cloud-Action tier. **Rewrites Invariant 53 in place** (high risk computed per-change on `(PR, head SHA)`, never a static per-repo flag). Names the **ADR-0086-deferred dual-pass worker substrate** (second pass + `Join-ReviewFindings` synthesis + contrarian fallback — currently unbuilt dead code) as a **hard prerequisite** for enforcement. LLM-judged risk triage rejected as out of scope (deterministic, auditable, $0-cloud-cost gate only). Amends ADR-0044 D8 / ADR-0086 D8 in part (the *definition* of high risk only; the dual-CLI substrate and advisory posture are untouched). Four implementing packets (catalog + invariant; cloud scorer; worker dual-pass substrate; worker trigger + enforce); `hive-sync`/`node-audit` catalog drift-check tracked as a follow-up. |

## Statuses

- **Proposed** — Under discussion, not yet decided
- **Accepted** — Decision made, implementation in progress or complete
- **Superseded** — Replaced by a newer ADR (link to replacement)
- **Rejected** — Considered and explicitly declined

## Creating a New ADR

Use the `adr-composer` agent or create manually:

1. Copy the pattern from an existing ADR
2. Use the next sequential number: `ADR-{NNNN}-{kebab-case-title}.md`
3. Include: Status, Date, Deciders, Sector, Context, Decision, Consequences, Alternatives Considered
4. Add a row to this index
