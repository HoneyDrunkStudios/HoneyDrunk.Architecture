# Workers

Operator-machine automation that is governed by Architecture ADRs but is not a deployable Grid Node.

- [`grid-agent-runner/`](grid-agent-runner/) — ADR-0086 scheduled agent runner for PR review, post-merge audit, `hive-sync`, and Lore jobs.
